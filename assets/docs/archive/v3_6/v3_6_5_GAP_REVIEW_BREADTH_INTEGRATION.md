# v3.6.5 — Breadth review: INTEGRATION lens (QA pre-flight)

**Verdict: READY.** The triplet content is byte-identical to the v3.6.4 both-lenses-cleared copy; the v3.6.5 patch bump (version-header bump + REVIEW_PROMPTS courier-repair + one surgical TESTING static-gate wording fold) is applied completely and consistently, and every system-fit invariant still holds against the current `dev` substrate.

**Scope.** Fresh end-to-end read of the CONSOLIDATED triplet, verifying the delta still FITS the base (function/cron budget, `is_test` isolation, sold-policy consistency, GPT char cap), with base assumptions checked directly against the live `dev` repo. Advisory breadth pass, not the formal gate.

## Integrity — intact + byte-coherent
- **Version bump complete + consistent.** All three docs read "consolidated at v3.6.4; v3.6.5 QA pre-flight bump"; the two addenda cross-reference `v3_6_5_IMPLEMENT.md` / `v3_6_5_ADDENDUM_DESIGN.md`; the IMPLEMENT required-reading + directory note point at the `v3_6_5_*` addenda; the ledger pointer bumped 39-54 → 39-56. No stale `v3_6_4` filename pointers survive (the only remaining "v3.6.4" strings are correct *historical* references to the consolidation event).
- **Invariants block (IMPLEMENT ~:67-81)** — present + unchanged: no new function (11/12), no new cron (1/1), sold-policy consistency, `is_test` isolation, auth unchanged, no hard delete, integer cents, brand separation, GPT 8000 cap, reduced-motion, cutover-untouched.
- **5 shared-file coordination bullets (~:38-51)** — all present + byte-coherent: `api/products.ts` WS2→WS4→WS8 (active_sale · coupon · activity bracket; validation-split-first) · `api/product-feed.ts` WS2+WS7 → ONE `feedAdmin` client, both jobs inside ONE `isCronRequest` gate · `api/orders.ts` WS3+WS8 single-definition `actor` · `shop.js`/`homepage.js` WS6→WS4→WS9 one merged block (§4.5.b/§4.5.f/§9.2 = pointers) · `main.js` §4.3.b + §4.7.0 share the one `DOMContentLoaded` anchor.
- **Named load-bearing fold blocks** — all present + coherent: PostgREST `.or()` runtime-gated try/catch fallback + once-per-cold-start `console.warn` (§2.6) · WS5 §5.4c.i re-role diff (openedRoles baseline · sequential gallery-NN splice · partial-failure recovery — all three) · STACK-AND-ERROR `wirePromo` skeleton (remove-first → apply → best-effort re-apply on throw → toast) · WS7 `isCronRequest` gate + `reconcileOrders` (26h window, `is_test`-scoped, `feedAdmin` reused, `RECONCILE_ALERT_EMAIL||ORDER_NOTIFY_EMAIL`, "Reconciliation OK" log matching TESTING item 27).
- **Sold-policy all FOUR enforcers** named + consistent (WS6 §6.5): `computeState()` (qty0-only, no `!available→sold`) · storefront gate (`published && quantity>0`) · `record_sale` (`available=quantity>0`) · `checkout.ts:79/:205` strict AND-gate 4th (fails safe). Legacy `available:false`+stale-`quantity` backfill sequenced in TESTING preflight.

## Base assumptions verified against live `dev`
11 `api/*.ts` (one slot free); 1 cron (`/api/product-feed`); `admin.js` present as port-source; latest migration `20260616000001` (the three new `20260701*`/`20260702*` sort after); GPT base exactly **7787** bytes (+201 = **7988/8000**); `active_sale`/`activity`/`scheduled_publish_at` genuinely absent from `products.ts` (real delta, zero collision). Nothing dropped or incoherent.

## Findings
- **F1 — polish, FOLDED at v3.6.5 (was pre-existing, NOT a regression).** The TESTING static gate asserted a blanket "every `summary`/`description` < 300 chars," but WS10 §10.1b (IMPLEMENT :3557) intentionally ships an `editProduct.scheduled_publish_at` schema description at ~665 chars (and `available` ~281); the IMPLEMENT itself (:3520) frames 300 as a *soft* cap with "no total cap," and GPT Action schemas impose no per-description hard limit. A literal reader running the gate would false-fail on an intentionally-long description. **Fold applied:** the gate wording is scoped to the two on-page-budget additions (createCoupon 225 + auto_apply 206) and reworded as a soft cap, with the editProduct exception named (integration-F1, ledger 56). No `is_test` leak, no budget/invariant violation, no added function/cron, no sold-policy inconsistency, no over-cap, no stale code `file:line` pointer found.

## If you fix one thing
Nothing load-bearing remains — the single non-breaking touch (the TESTING static-gate <300 wording vs the 665-char schema description) is folded. The triplet fits the substrate cleanly and the patch bump is sound.
