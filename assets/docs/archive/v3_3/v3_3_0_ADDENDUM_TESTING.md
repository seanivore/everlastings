# v3.3.0 — Testing Addendum

**Addendum to**: `v3_3_0_IMPLEMENT.md` (same version; bumps in lockstep).
**Where**: the **dev preview** URL (NOT localhost) — Preview env, Preview `PRODUCT_API_KEY`, Deployment Protection (SSO) **off** for the run so Stripe webhooks + the GPT can reach it. Stripe **test mode**. The GPT pointed at the dev preview with the Preview key.
**Stripe prerequisites:** the preview webhook endpoint must subscribe **`charge.refunded`** (else the reconciliation check, item 8, never fires) in addition to `checkout.session.completed`. **How:** Stripe Dashboard → Developers → Webhooks → click the preview endpoint → **+ Add events** → search `charge.refunded` → check it → Save (repeat on the production endpoint at cutover). Buyer refund emails are **Stripe-sent and live-gated** — do **not** assert email delivery in test mode.
**Bar**: every assertion green before promotion. Design is tested + feedback'd like functionality (concrete default + render-tune with Sean). Real content is never a gate — production-grade placeholders stand in.

**Before you start (runtime preconditions):**
- [ ] **Seed 3 live test products** in the dev preview (test-mode prefixed `test/…`) — the headline multi-cart test (item 1 + #3) needs three different live pieces to put in one cart. If the preview catalog is empty/sparse, create them first (via /admin or the GPT).
- [ ] **Repoint the Custom GPT at the dev preview** — in the GPT builder, set its Action server URL to the Preview URL and the auth to the **Preview** `PRODUCT_API_KEY` (else the GPT tests hit production). A 2-minute manual step that's easy to forget; do it before any GPT-surface item.
- [ ] **Subscribe `charge.refunded`** on the preview webhook endpoint (the Stripe-prerequisites step above) — item 8 never fires otherwise.
- [ ] Confirm the preview is `VERCEL_ENV !== 'production'` so `is_test` reads/writes stay under `test/…` (the CDN-prefix assertions assume it).

> **Static gate (first):** `npx tsc --noEmit -p tsconfig.json` clean · `ls api/*.ts` count unchanged (refund folds into `orders.ts`, attach into `upload.ts` — no new function) · `node --check` clean on `admin.js` / `product.js` / `homepage.js` · `vercel.json` valid JSON with the 2 new rewrites (`/api/orders/:id/refund`, `/api/upload/attach`), and the **refund rewrite precedes `/api/orders/:id`** (`grep -n '/api/orders/:id' vercel.json` → the `…/refund` line first, else it falls through to the PATCH-only route → 405) · **`v2_0_0_GPT_INSTRUCTIONS_TRIMMED.txt` = the Phase 3.9 final text verbatim and `wc -c` < 8000** (verified **7788 / 8000**, 212 headroom; over the cap → the GPT silently truncates its own instructions) · GPT schema valid + every `summary`/`description` < 300 chars (count, don't estimate) · the `record_sale` migration is applied AND the cutover data-fix is gated by mechanism, not memory: the destructive `quantity=0` UPDATE **ships COMMENTED** in the migration (`grep -n 'update products p set quantity = 0' supabase/migrations/*inventory_decrement.sql` → the matching line is comment-prefixed `--`), so `supabase db push` applies ONLY the safe `record_sale` function and never auto-zeroes stock; the operator then runs the Phase 6.1 SELECT, confirms every row is genuinely sold (not a paused-with-stock piece), and uncomments + runs the UPDATE **by hand, once** (a doc-only "remember to eyeball" gate can't protect a template "User" with a multi-stock catalog; WS6 — Phase 6.1) · **`package.json` pins `engines.node` to `">=20"`** AND `grep -n 'runtime' vercel.json` shows no Node-18 override on the upload function — so `new File`/`Buffer` in `handleAttachedRefs` are present at runtime (`tsc` can't catch a missing runtime global) · **`grep -n 'upload-status' assets/js/admin.js` → 0** (P3d removes `#upload-status`; `openEditor`'s reset is repointed to clear `.upload-zone .zone-msg`, else the stale `$('upload-status')` throws a `TypeError` on **every** editor open — runtime-only, invisible to `tsc`/`node --check`) · **`curl -I` the lottie CDN URL pinned in DESIGN §5.1** (`bodymovin/5.12.2`) returns **200** — a 404 on a `defer`'d script is silent (no console error) → the hero falls back to the static `<h1>` and item 30's write-on fails with no obvious cause (swap to a present version like `5.10.x` if cdnjs lacks it).

---

## WS1 — Refund (both surfaces — amount-based)

> **🎯 Headline — multi-item cart, no over-refund.** Build a cart of **3 different pieces** (one checkout → 3 sibling `orders` rows sharing one `stripe_payment_intent`) and complete a test purchase. On any one order's card → **Refund this purchase…** → the panel lists **all 3 pieces** with their charges → check **one** → the amount fills to that piece's charge → Refund → in the Stripe test dashboard the refund is **only that line's amount, NOT the cart total**; **only that piece** flips `refunded` + relists; the **other two stay `completed` + available** + still buyable. Then check **two** → both refund + relist. Then **edit the amount down** (a restocking deduction) with a piece still checked → the smaller amount refunds, the checked piece still relists. *(A Stripe refund is an amount against the PaymentIntent and one cart is N orders — a `{ payment_intent }`-with-no-amount call would silently refund the whole cart.)* **Sub-subtab independence:** the panel **fetches the cart's full sibling set by PaymentIntent** (`GET /api/orders?payment_intent=…`), so **mark ONE of the 3 pieces shipped first** (so the siblings straddle Needs-Shipping/Shipped), then open Refund **from the default Needs Shipping subtab** → the panel STILL lists all 3 pieces (not just the unshipped ones). It must not silently under-list a partially-shipped cart from any subtab.

1. **/admin, published-but-sold piece — from the All Orders subtab** (the orders subtab strip is **Needs Shipping · Shipped · All Orders** — `admin/index.html:244-247`; a refunded order drops out of Needs Shipping's `status=completed` filter, so use **All Orders**, where it persists regardless of shipping state — or **Shipped** if the piece was shipped): a test order exists for it → Orders tab → **Refund this purchase…** → the **refund panel** opens showing the piece + its charge, **pre-checked**, amount **pre-filled** → Refund → confirm → "Refunded"; the order shows the **Refunded** pill (`.pill.refunded`) and the relist prompt fires → accept → "Refunded + relisted"; the product is **available** again on `/shop` **and a real test checkout completes on it** (buyability restored, not just the listing). Verify the refund + its amount in the Stripe test dashboard.
2. **/admin, archived piece (incl. archived-AND-sold):** refund (panel, piece checked) → relist prompt → accept → the piece **unarchives AND its stock is restored** (`quantity + 1`) so it's back on `/shop` **and buyable**; decline on another → stays archived/down. (Exercises the both-axes relist.)
3. **GPT — single piece:** "refund <buyer>'s order for <product>" → it **reads back** piece + amount + buyer and waits → confirm → `refundOrder {id}` → reports refunded + **always offers** to restore (wording by state — re-list a down piece, +1 an in-stock one) → "relist" → `editProduct {id:<product_id>, available:true, quantity: relist.quantity + 1}` (and/or `unarchiveProduct {id:<product_id>}`) runs.
   - **GPT — multi-piece (proves `relist[]` is well-formed):** on the 3-piece cart from the headline test, "refund <buyer>'s order — two of the three came back" → it confirms WHICH two + passes `amount_cents` (summed) + `relist_product_ids:[both ids]` → **assert it then fires TWO separate relist offers, one per `relist[]` entry**, each naming its own piece (not one merged prompt) → accept both → each piece relists with its own `{id:r.product_id}`. (If `relist[]` ever came back malformed the prompt would silently never fire — this is where that surfaces.)
4. **Idempotency:** trigger the SAME refund twice fast (double-click / retry) → the Stripe dashboard shows **exactly one** refund for that amount on the payment intent; the second response is **409** (already-refunded) **or** a **200 with Stripe's idempotent same refund** — never a second distinct refund. (Key = `refund-<pi>-<amount>-<ids>`, so a **different** piece/amount on the same purchase is NOT blocked — verify a second, distinct per-line refund succeeds.)
5. **Guards (incl. amount):** refund a non-existent id → 404; an already-refunded order → 409; an order with no `payment_intent` → 409; an undeterminable amount → 400; an amount over the refundable balance → Stripe errors → **502** surfaced.
6. **Always-offer relist, per returned piece:** a piece **still `available`** (multi-qty, units left) → *"Increase <title>'s available quantity by 1?"* → accept → `quantity` **+1** (decline → unchanged, no error); a **sold-out** piece → *"Re-list … and make it available for purchase again?"* → accept → quantity 0→1 + available true.
7. **Partial / goodwill (in-app now):** in the panel **check nothing** (or uncheck) and type a **smaller amount** → that amount refunds in Stripe, **no piece relists**, the order **status is unchanged** (nothing was returned). The "open in Stripe" deep-link is still present as a backstop.
   - **Full-amount goodwill — status DOES flip:** check nothing but type the **FULL purchase amount** → Stripe records a full refund → `charge.refunded` reports fully-refunded → the order **flips to `refunded`** (item 8) even though nothing was marked returned, and **no relist prompt fires** (no pieces). **Don't assert "status unchanged" here** — a full-amount refund is a full refund by definition. (The GPT instruction warns her of this before confirming; the piece stays sold/`available:false` since it wasn't marked returned.)
8. **Webhook reconciliation + `is_test`:** a **full-PI** refund's `charge.refunded` flips **every** sibling order on that PI to `refunded` (idempotent with the handler's optimistic flip); an **intermediate partial** doesn't fire fully-refunded, so the **handler** owns that piece's flip — no error either way. `is_test` scoping holds: on the test preview a prod order id is **not** found (404).
   - **`is_test` on the by-PI GET:** request `GET /api/orders?payment_intent=<a known LIVE PI>` from the preview → returns `{ orders: [] }` — the new PI filter chains AFTER the base `is_test` filter (Phase 1.1c), so a live cart's siblings can never leak into a test-preview refund panel. (The headline "no over-refund" relies on the GET returning the *right* sibling set; this asserts it can't return the wrong-env one.)

## WS2 — Coupons in /admin + human dates

9. **Create (store-wide):** Coupons tab → 20% off, code blank, "Ends after" = a date → read-back confirm → created; the **Running sales** list shows it with a **plain-date** `ends …` (not a raw timestamp).
10. **Create (product-scoped, multi-select):** type a term in the scope search → check **two+** published products → type another term → check more (earlier checks **persist** across searches) → create → `product_ids` carries **all** checked `stripe_product_id`s and the list row shows "N products"; an unpublished/archived product is **not** offered. (Strict parity with the GPT's multi-product scope.)
11. **Amount + min + cap:** `$ off` 5, min $50, max 100 → values land as **cents** (500 / 5000) and the cap **and the minimum** (`· min $50.00`) show in the list (the min is no longer write-only).
12. **Deactivate:** **End sale** → confirm → it leaves the list; Stripe shows the promo `active:false`.
13. **GPT parity + dates:** `listCoupons` returns `expires_display`; the GPT relays the **plain date** and never decodes a timestamp. createCoupon → the GPT **reads the full terms back** before creating. **Readback parity:** create a coupon **with a minimum-order** on one surface, then list it on BOTH — the minimum shows back in /admin (`renderCoupons` → `· min $NN.NN`) AND the GPT — which relays `min_display`/`amount_display` (plain dollar strings) so it states "$50.00 minimum", not a raw `5000`. Confirm the GPT speaks the right **dollars** (a 100×-off readback must FAIL this gate — field-visibility alone is not enough), and that no settable coupon attribute (code · percent-or-amount · scope · expiry · cap · **minimum**) is write-only on either surface.

## WS3 — Chat-attach upload + admin media UX

14. **Attach 1 photo (desktop ChatGPT) — proves the `openaiFileIdRefs` round-trip:** GPT calls `uploadImages`; OpenAI substitutes the string-array placeholder with file objects, the handler fetches each `download_link`, and one CDN url comes back; it lands under `test/<slug>/test_hero-<slug>.webp` (test mode prefixes `test_`). If the Action validator ever rejected the placeholder or the substitution stopped firing, **this is where it surfaces** — the reason the schema keeps the documented `items:{type:string}` shape. This round-trip also exercises the Node-20 runtime globals `new File([bytes])` + `Buffer` in `handleAttachedRefs` (which `tsc` can't verify); a successful upload confirms they're present on the Vercel preview runtime.
15. **Attach a 7-photo batch:** 7 uploads; first = `hero`, rest = `gallery-0N`; `createProduct` succeeds reusing the hero url for thumbnail.
16. **`roles[]` honored:** attach 3 + tell the GPT which is the hero / a detail → returned roles match.
17. **> 10 / expiry / fallback:** > 10 in one call → friendly "batch it"; a stalled (expired) link → friendly "re-attach"; blank `openaiFileIdRefs` → GPT falls back to asking for a Drive/direct link (`uploadImage` still works).
18. **Mobile attach (iOS/Android ChatGPT app):** the headline cross-device case — attach from the phone, product builds.
19. **Alt text:** the GPT sets a descriptive `alt` on every image + `thumbnail_alt` (no blank alts on the rendered page).
20. **Filename/role:** the frontend categorizes hero vs gallery correctly (role read from the `{role}-{slug}` filename) — no mis-placed images; nobody renamed a file.
21. **Admin media editor:** open a product → the **structured MP4/YouTube rows** show (not raw JSON); add a clip, set GIF-like vs click-to-play + poster + alt, Save → reopen → round-trips identically; the page renders it (MP4 before YouTube). A YouTube row hides the video-only options. **Controls toggle + round-trip:** the video row carries a **`controls`** checkbox — the GIF-like "no buttons" vs click-to-play state /admin couldn't set before (the GPT could, conversationally). Uncheck it on a clip, Save **without other edits**, reopen → assert `controls` is **still `false`** (`collectMedia` serializes it; `openEditor` rebuilds the checkbox from `media[i].controls`). No Supabase-Studio seed needed — the value is owner-settable right in the editor.
22. **Admin image previews + coverage — all 7 roles:** paste a URL for **each** of the 7 roles in turn (`hero`, `gallery-0N`, `detail-0N`, `thumbnail`, `seo_thumbnail`, `checkout_image`, `video-0N`) → each shows a thumbnail + a role tag matching the filename's `m[1]` (incl. `checkout_image` + `seo_thumbnail`); the **hero / gallery N/5 / thumbnail** coverage hint updates live; and the **thumbnail ✓ is honest** — a hero-only product (no explicit thumbnail) saves with the hero reused as the thumbnail, so publish doesn't reject after a green check. **End-to-end via the upload ZONE, not just the preview/tag:** uploading a file through the **`seo_thumbnail`** and **`checkout_image`** zones actually *sets* those column-backed attributes (`#p-seo-thumbnail` / `#p-checkout-image`) — after publish, the product page carries the OG/social image (`seo_thumbnail`) and Stripe checkout shows the uploaded 1:1 image (`checkout_image`), NOT a silent fallback to the thumbnail.
23. **Auto-skip_transform:** upload an MP4 via the admin control **without** ticking skip_transform → it's stored as-is (no Cloudinary crop); an image still transforms.

## WS4 — Admin polish (neutral/template)

24. **Brand-neutral + tokens:** the `:root` token system applied; storefront brand untouched. **No *bare* raw hex literals remain** — grep `admin/index.html` + `assets/js/admin.js` for `#[0-9a-fA-F]{3,6}` and confirm every hit sits **inside a `var(--token, #hex)` fallback** (the deliberate WS1-3 render-before-§4.1 pattern — `updateCoverage`/`addImageRow`/`addMediaRow` + the WS1-3 inline styles), never as a bare value. The **one** allowed bare literal is the P4 `.skeleton` shimmer gradient's highlight stop (`#e9ecef`, a one-off gradient midpoint between two `var(--c-surface-2)` stops — not a themeable color).
25. **State visibility:** product cards show exactly **ONE** `.pc-badge` per card (live / draft / edits / sold / archived) — no second `available`/`sold` pill, and the old `.pill.unsent`-for-sold + `.pill.shipped`-for-live reuses are gone; archived dimmed (`is-archived`).
26. **Nav (P0):** clicking into a product shows a clear **← Products** return; the browser Back button is no longer the only way out; the active tab is obvious.
27. **State-filter (P0):** the product list filters by **All / Live / Draft / Sold / Archived**; filtering to a state with zero matches shows **"No products match this filter."** (not the empty-catalog "No products yet").
28. **States, mobile + a11y:** skeleton/loading + empty + error states render; the panel is usable at a phone width (≤640px) — forms stack, no horizontal overflow, and the **5-cell `.img-url-row`, the structured media editor's `.media-row__head`, + the upload zones** collapse to a single column (verify at **360px**: the URL input + Remove are full-width + tappable, not crushed into the 40px thumb column, **and the MP4/YouTube clip rows don't overflow the screen edge**). **A11y:** each upload zone's file input is **wrapped inside its `<label>`** (clicking the zone name opens the picker; a SR announces the role), and the structured media inputs (`.m-url`/`.m-poster`/`.m-alt`) carry `aria-label`s.
29. **Render-tune:** Sean's eye on the live look (accent, spacing, type) — adjust to taste.

## WS5 — Homepage experience

30. **Lottie title:** the hero title writes itself in on load (lottie-web SVG); a real `<h1>` is in the DOM (view-source / a11y tree) for SEO; `prefers-reduced-motion: reduce` → static styled `<h1>`, no Lottie; a blocked/404 JSON → static `<h1>` still shows (no blank hero).
31. **Old-film hero:** the re-rendered MP4 (versioned CDN key) plays with the grade/grain/flare; the poster re-graded to match; parallax / overlay / spotlight / edge-glow + the reduced-motion poster fallback all still work; `npx hyperframes lint` clean on the composition.

## WS6 — Inventory (stock decrement on a sale)

32. **One-of-a-kind (qty 1):** buy it → the webhook runs `record_sale` → `quantity` 1→0 and `available` flips false → shows "Sold". Identical to pre-WS6 behavior.
33. **Multi-stock (qty ≥ 2):** set a piece's quantity to 2 → buy one → `quantity` 2→1, **`available` stays true**, still listed + buyable; buy the second → `quantity`→0, `available`→false ("Sold"). Proves a sale no longer strands extra stock.
34. **Atomicity:** two near-simultaneous completions on the same qty-1 piece never drive quantity negative (`greatest(…,0)`); the second is a no-op at 0.
   - **`record_sale` decrements by multiplicity (future-proofs a multi-buy cart):** call `record_sale(array['<id>'::uuid,'<id>'::uuid])` against a qty-3 row → quantity 3→1 (decremented by **2**, the grouped-count form — NOT a flat −1). Confirms the RPC stays correct if a future cart ever passes a duplicate id for a quantity-N line. (Cleanup: reset the quantity.)
35. **A sale never archives / never touches Stripe:** after a sale, `archived_at` is unchanged and the Stripe Product/Price stay `active` (the piece is re-sellable with NO new Price — only our `available`/`quantity` gate it).

## Cross-cutting invariants

- `npx tsc --noEmit` clean after every TS edit · CORS unaffected (`*.vercel.app` allowlist) · `is_test` keys stay under `test/…` on preview · the go-live (`main`) version is untouched throughout.
