# v3.5.2 — Gap Review · Angle D (design-correctness) · ROUND 2 (convergence check)

**Scope.** Confirm the round-1 + breadth DESIGN fixes landed right and flag only NEW render/design gaps. Read end-to-end: `v3_5_2_IMPLEMENT.md`, `v3_5_2_ADDENDUM_DESIGN.md`, `v3_5_2_ADDENDUM_TESTING.md`, the design research (`brief.md`, `FEEDBACK_v1.md`, `controls.html`+`tokens.css`, `out/`, `reference/`). Verified against the actual `out/` byte-source + the storefront `styles.css`. Ledger respected (esp. the 5-color LED — NOT re-flagged).

**Verdict: READY TO BUILD.** The design layer is render-correct across all three docs and the `out/` source. Every round-1/breadth fold I was asked to converge on is right and internally consistent. One NEW finding, LOW severity, is a documentation-completeness nit in the DESIGN §A surgical-edits enumeration — it does not affect any render.

---

## Convergence confirmations (the round-1/breadth DESIGN fixes — all CORRECT)

- **D2 `--staged`→`--waiting` sched-chip fix — CORRECT and load-bearing.** `out/products.html:288` `.sched-chip` uses exactly **three** `var(--staged)` (grep-confirmed = 3), and `--staged` is defined **nowhere** in `out/` (only `--waiting`/`--live`/`--draft`/`--sold`/`--archived`/`--danger` exist in `portal.css:29,76`) — so today the chip's `color-mix(in oklch, var(--staged), …)` is invalid → the chip renders with **no background**. The `.sched-chip` is genuinely emitted (`products-app.js:410`, when `p.scheduled_publish_at` is set — the schedule popover at `:472` is a real write, not a dead button), so this is a live render bug the fix closes. Mapping to `--waiting` (orange) is correct: scheduled/staged is the orange "waiting to go live" family. DESIGN §A's description ("three `var(--staged)`", "renders with no background", "orange `--waiting` state") is byte-accurate.

- **5-color LED — CONSISTENT across all four surfaces (not re-flagged; ledger-correct).** `out/portal.css:401-405` (`.led--live` green / `.led--edits` orange `--waiting`, `led-pulse 1.2s` / `.led--draft` yellow, `led-pulse 1.7s` / `.led--sold` blue `#297fb4` steady / `.led--archived` purple-gray `#83718a` steady) ↔ `products-app.js:16-22 computeState()` (precedence `archived > draft > edits > sold(qty0) > live`, **no `!available→sold` branch**) ↔ `:90 ledFor()` (renders `.led--{state}` for all five) ↔ IMPLEMENT locked-decision **line 99** (same five, "both the LED color AND a tab, not either/or") ↔ DESIGN §C.1/§E ↔ TESTING item 5. Edits blinks faster than draft (1.2s vs 1.7s) = the "staged is the more urgent nudge" intent. The separate segmented-filter `.dot--*` classes (`portal.css:373-376`) also carry the full 5-state palette. No inconsistency.

- **WS8 §8.3 blink/badge decouple — byte-anchored + render-complete.** In `out/`, `portal.js:147,154` mountShell drives BOTH the `data-alert` blink AND the numeric `.badge` from the **same** `badge` var (coupled). IMPLEMENT Phase 8.3a/8.3b (`:3332-3366`) correctly splits them: `data-alert ← unseen_count > 0`, `.badge ← needs-shipping count`, via a shared `P.refreshOrdersSignal()` that runs on mountShell pages **and** the Products static rail (`products.html:321` hardcodes `data-alert` on the Orders rail item; the helper's `el.toggleAttribute("data-alert", unseen > 0)` corrects it). Crucially `:3360` `else if (b) { b.remove(); }` clears a stale mock badge when `needs===0` — so the two are truly independent and no stale mock "2" survives. Query selector `[href="orders.html"]` matches the literal markup attribute (the 308 clean-URL canonicalization is transparent to it). Reduced-motion is honored (`tab-alert` blink is disabled by the universal `@media (prefers-reduced-motion: reduce)` block, `portal.css:527-531`). Render-correct.

- **DESIGN §A surgical-edits list — ACCURATE for the 5 enumerated items.** Each verified against `out/`: robots meta ABSENT in all four shells (→ WS1 add is genuine; titles already correct so no title edit needed); `product_type` picker currently `["miniature","printable","storybook"]` at `products-app.js:364` (→ WS2 narrows to `["miniature"]`); Sold-tab `data-alert` present at `products-app.js:98` driven by the `unseenOrders=2` stub (→ WS8 removes); Delivered pill present at `orders-app.js:69` + `.tpill--delivered` CSS at `orders.html:51-52` (→ WS3 removes, and no code writes `status='delivered'`); the `--staged`×3 fix (above). All five match. See Finding D-R2-1 for the one omission.

- **State-color system + KILL list — HOLD.** Color reserved strictly for state (LED, field `data-ring` green/red/yellow via `readiness()`/`refreshGate()` at `products-app.js:47,448,539-543`); products are rows not tiles; pills/LEDs are color-only (no words); no portal-name header; helper text lives in `.tip` label tooltips. **Brand separation intact:** no plum/serif/`--color-plum`/display-serif token leaks into `out/` (the only "serif" hit is the generic `sans-serif` fallback in `--font-ui`), and the §D storefront CSS uses **only** storefront tokens (no portal `--accent`/`--ink`/`--waiting` leak).

- **Mobile-primary / reduced-motion / a11y — CONFIRMED.** Inputs are `font-size:16px` on mobile (`portal.css:298`) tightening to 14px at `≥860px` (`:307`); the media-modal inputs re-assert 16px on mobile (`products.html:290`). Reduced-motion universally collapses transitions/animations + press-transforms + skeleton shimmer (`portal.css:527-531`) — the LED pulse, tab-alert blink, breathe, and popup entrance all respect it. Focus-visible rings and the honest computed publish-gate (red ring + plain-language "what's missing") are present.

- **NEW storefront components (struck-`%`, top bar, once-only popup) — SPECIFIED CONCRETELY, render-correct.** IMPLEMENT §4.3.c/4.3.d (`:1276-1366`): `.price-sale`/`__was`/`__now` + `.sale-bar`/`.sale-pop` reference **22 storefront tokens — every one exists in `styles.css`** (checked individually). z-index math is right: popup at `calc(--z-modal + 10)` = 210, below `--z-cookie` = 400, so the cookie banner is never covered. The once-only gate is code-scoped correctly — `if (localStorage.getItem('everlastings.saleSeen') === sale.code) return;` and `setItem(..., sale.code)` on dismiss, so a NEW sale (different code) re-announces once (matches §D.2 + TESTING item 16). Popup entrance animation is gated behind `@media (prefers-reduced-motion: no-preference)` (`:1362`). Struck only on percent sales; sold items render plain; JSON-LD untouched. All storefront (warm-plum) tokens — no portal token in sight.

---

## NEW findings (ranked)

### D-R2-1 — LOW · DESIGN §A surgical-edits list omits the `products.html:334` View-Site href swap
DESIGN §A presents its surgical-edits list as exhaustive ("the ONLY diffs; everything ELSE is verbatim … enumerate them so a reviewer never mistakes one for a stray change") and states data-wiring happens "**without changing markup or class names**." But WS1 also swaps a **markup attribute**: `out/products.html:334` `<a class="rail__item rail__foot" href="https://everlastingsbyemaline.com" …>` → `P.siteUrl()` (env-aware). This edit IS documented — IMPLEMENT open-item **line 418** and behaviorally in DESIGN **§D.3 line 122** — but it is **absent from the §A enumerated list**, which is the list an Angle-B byte-check reads. A byte-check reviewer diffing `out/products.html` against `admin/products.html` would see the changed href and, not finding it in §A, could flag it as a stray change.
- **Fix:** add one bullet to the §A list, e.g. *"WS1 — swap the static `products.html:334` View-Site href (hardcoded prod) → env-aware `P.siteUrl()` (or route the products rail through `mountShell`) — INTEGRATION §165 / IMPLEMENT §WS1."*
- **Secondary (optional):** WS8 Phase 8.3a **adds** `P.refreshOrdersSignal` to `portal.js`. It's a JS-helper addition (no markup/class change → arguably within allowed "no-op → endpoint" wiring, not a "surgical edit"), so it's defensible to leave out — but for a truly-exhaustive §A a one-line note would prevent the same "stray addition?" question during byte-check.
- **Not a render gap.** The View-Site link renders and works either way; this is doc-completeness only.

### Open NEEDS-VERIFY flags carried forward (pre-existing — NOT new, listed for the trail)
- **§C.3** — per-field recommended/SEO char-count targets: confirm they're populated for every field the FEEDBACK §8.7 rule covers (not only SEO title/description); a field with no target should show a bare count, not a `0/limit` that reads as always-over. (Already flagged in-doc.)
- **§D.3** — `PORTAL.env()` hostname split (`*.vercel.app`/`localhost`→Test, `everlastingsbyemaline.com`→Live) vs the actual Vercel deploy topology. (Already flagged in-doc; "the one line to adjust" if hostnames differ.)

---

## If you fix one thing
Add the `products.html:334` View-Site href swap to the DESIGN §A surgical-edits enumeration (Finding D-R2-1) — it's the single place the "these are the ONLY diffs" list is incomplete, and closing it means the Angle-B byte-check has a clean, exhaustive diff manifest with zero "is this a stray change?" ambiguity. Everything else in the design layer is render-correct and consistent.

## One-line verdict
**READY TO BUILD** — the design layer renders right; every round-1/breadth fold converged; the sole new finding is a LOW doc-completeness nit (§A omits one already-documented href edit), not a render regression.
