# 2026-07-02 — Session log (v3.5 build-doc authoring + in-session gap-review loop)

**Driving**: author the v3.5 build docs from the Claude Design portal handoff + the v3.5 backlog, then run the in-session pre-gate loop (self-gap → breadth → A/B/C/D subagents) toward hand-off for the formal Agent-SDK gap-review gate.
**Type**: planning (build docs; no code touched — `api/`, storefront, prod all untouched).

## What was produced (all in `assets/docs/archive/v3_5/`)

- **`v3_5_2_IMPLEMENT.md`** — 10 workstreams (WS1 portal shell/routing/auth · WS2 Products + sold-policy + scheduled-publish · WS3 Orders + refund-preserve · WS4 store-wide sale + struck pricing + top-bar/popup · WS5 media rebuild · WS6 storefront bugs · WS7 webhook/money · WS8 activity log + seen/unseen · WS9 buy-on-tile info-scent · WS10 GPT+docs parity). Byte-anchored; drafted by 10 focused subagents, spliced, self-gap-reconciled (shared-file edit coordination for `products.ts`/`product-feed.ts`/`orders.ts`).
- **`v3_5_2_ADDENDUM_DESIGN.md`** + **`v3_5_2_ADDENDUM_TESTING.md`** — the design + testing thirds (lockstep version, always in gap-review scope).
- **`v3_5_2_REVIEW_PROMPTS.md`** — the paste-ready A/B/C/D gate driver + a 30-entry "Settled — do not re-raise" ledger. (Header currently reads v3.5.1.)
- **`v3_5_0_GAP_REVIEW_{A,B,C,D}.md`** — round-1 cold-gate findings (kept standing, the rigor trail).

## Loop state

- **Round-1 cold gate (A/B/C/D):** A = NEEDS ANOTHER PASS; B/C/D = NARROW; build **byte-faithful**, base fits. Validated + folded → **v3.5.1** (14 folds; critical one = the `product-feed.ts` `is_test` isolation fix + single-`feedAdmin`-client merge; + the 5-color LED doc correction — the delivered `out/` uses blue-sold/purple-gray-archived LEDs per FEEDBACK §3, my ledger had been stale).
- **2 breadth regressions (owner-journey + integration):** both NARROW; fold mechanics verified clean (budget 11/12 + 1 cron held, three sold-policy enforcers agree, boot anchors byte-match). Surfaced the two fold-induced regressions below.
- **v3.5.2 fold — COMPLETE** (committed `fe2659e`, pushed to `dev`; living docs renamed `v3_5_0_*` → `v3_5_2_*`). Both fold-induced bugs fixed (`§4.7` `?code=` vs store-wide auto-apply now mutually exclusive; `§6.5a–d` null-safe `quantity != null ? qty<=0 : !available` sold gate on card / PDP buy-gate / JSON-LD / related-card) **plus the full tail:** WS8 §8.3 byte-anchored `PORTAL.refreshOrdersSignal` (per-page blink ← `unseen_count`, real needs-shipping badge, Products static rail wired); WS10 §10.1b `editProduct` schema (take-down→draft note + `scheduled_publish_at`; schema-only, GPT `.txt` still 7988/8000); TESTING protection-OFF self-call; two small guards (preview-Sold suppression, title-before-media).

## Round 2 complete — validated fold list for v3.5.3 + round-3 kickoff (HANDOFF POINT)

**Round-2 A/B/C/D verdicts:** **C (integration) + D (design) = READY TO BUILD**; **A (cold) + B (fidelity) = NEEDS ANOTHER PASS (NARROW)**. B verified every named fold delta byte-matches the tree. Full findings stand in `v3_5_2_GAP_REVIEW_{A,B,C,D}.md`. Handing off here (usage ~25%) rather than fold on fumes — a half-applied fold would be mixed-truth.

**Validated fold list → v3.5.3** (2 load-bearing, rest small; already validated flag-don't-assert):
- **[LOAD-BEARING] B2-1 — `shop.js`/`homepage.js` WS4↔WS6 edit collision.** §4.5.b (WS4 struck price) + §6.5a (WS6 sold block) both rewrite `shop.js:139/141`; §4.5.f (WS4) + §6.3d (WS6 carousel) both rewrite `homepage.js:61`. The shared-file coordination section wrongly calls these "non-overlapping" — a mechanical build applies one and silently reverts the struck price (sale vanishes on 2 of 5 struck surfaces). **Fix:** add `shop.js` + `homepage.js` to the coordination list, order **WS6→WS4**, with ONE merged NEW per price line (sold-state + struck price both applied). Also gate the struck-price render on **`!sold`** (not `p.available`, §4.5.d) so a sold piece never shows a struck sale price. *(Double-confirmed — both round-2 A (#2) and B independently caught this collision.)*
- **[LOAD-BEARING] A2-1 — share link half-wired.** §4.6 builds `<siteUrl>/?code=CODE` (homepage root), but §4.7 `applyShareLinkCode` only reads `location.search` on `/checkout` → the param is lost before checkout. **Fix:** capture `?code=` site-wide in `main.js` on load → `sessionStorage`; `applyShareLinkCode` reads the stash (fallback to `location.search`).
- A2-3 — WS9 §9.2 "one of a kind" badge is prose-only: give it a concrete class/copy/CSS (mirror `.badge.badge-featured`/`.badge-sold` in `styles.css`; place in `card__media`).
- A2-4 — scheduled-publish silent-fail: gate the Schedule control on **publish-readiness** (passes `validatePublishRules`) and/or surface a failed-schedule notice ("nothing hides without explaining").
- A2-5 — move the §6.5b `?preview=` sold-skip guard from prose into the NEW code block.
- C1 — name the server checkout/reserve gate (`available===true && quantity>=1`) as the intentional strict **4th** sold-policy enforcer (doc framing; not "three").
- C2 — `refreshOrdersSignal` (§8.3): tighten the read to `GET /api/orders?status=needs_shipping` (still returns `unseen_count` + the smaller needs list).
- D-R2-1 — add the `products.html:334` View-Site href swap (`→ P.siteUrl()`) to DESIGN §A's surgical-edits list.
- B2-2 — §10.1b: note the `available` schema anchor is **editProduct-scoped** (`:127` + editProduct context; the token is non-unique — also at `:36/:91/:403`).
- Strike stale flags: **A2-7** (WS3 line-911 authHeader — already satisfied by WS1 §1.3b). **Drop as false alarms:** A2-2 (`getCartTotal` is in scope in checkout.js) + A2-6 (`.badge` class is correct per portal.css).

**Round-3 kickoff (after folding → v3.5.3):** run the 2 breadth subagents (owner-journey + integration) after the fold; then a scoped A/B round 3 (A+B were the open angles). C+D returned READY — re-run them only scoped, since the B2-1 collision + C2 touch C's lane and the WS9 badge touches D's. When a round returns clean/nits → **minor-bump to `v3_6/` (v3.6.0)**: copy the final v3.5.x living docs → `v3_6_0_*`, bump headers + cross-refs, leave final v3.5.x in `v3_5/`. That is the formal Agent-SDK A-Type gate kickoff.

## Round-2 fold APPLIED → v3.5.3 (round-3 pending)

**All round-2 validated findings folded; living docs renamed `v3_5_2_*` → `v3_5_3_*`.** Two load-bearing + 8 small, all byte-grounded against the repo:
- **[load-bearing] shop.js/homepage.js/product.js card-render collision** — the coordination section now lists `shop.js` + `homepage.js` (apply order **WS6→WS4→WS9**, ONE merged NEW per card block); §6.5a (shop) + §6.3d (homepage) are the canonical merged blocks (sold-state + struck price gated on `!sold` + the "One of a kind" badge); §4.5.b/§4.5.f/§9.2 are now **pointers**. `product.js` §4.5.d struck gate reconciled to a block-local `!sold` (sibling scope to §6.5b — not a line collision, per Angle-B). Line 46's false "non-overlapping" claim corrected.
- **[load-bearing] coupon `?code=` share link** — the link lands on the homepage root, so §4.7.0 captures `?code=` in **`main.js` → `sessionStorage`** (main.js loads on every page — verified); §4.7 `applyShareLinkCode` + the gate now read via a one-shot `readShareCode()` (stash first, `location.search` fallback). `main.js` added to WS4's file roster.
- **Small:** WS9 §9.2 badge made concrete (spec = **DESIGN §D.4** `badge-unique`, render in §6.5a/§6.3d, `!sold`-gated) · §2.4 Schedule control gated on publish-**readiness** + §2.6 cron backstop note (A2-4) · §6.5b `?preview=` guard folded into the code via `!previewToken` (A2-5) · C1 4th sold-policy enforcer named (`checkout.ts:79`/`:205`) · C2 §8.3 read tightened to `?status=needs_shipping` (unseen_count confirmed filter-independent) · B2-2 §10.1b `available` anchor noted editProduct-scoped (`:127`, non-unique) · D-R2-1 §A View-Site href-swap bullet added. Struck A2-7; dropped A2-2/A2-6 as false alarms (both added to the ledger's false-alarm classes). Resolved A Finding 3 in-doc: `getCart`/`getCartTotal` are `main.js` globals (`:104`/`:167`).
- **Ledger + tests:** REVIEW_PROMPTS ledger gained entries 31b/32b/33b + the three false-alarm classes; TESTING gained item 15b (share link end-to-end) + sharpened item 30 (merged-block coexistence).

**Round-3 (next):** run the 2 breadth subagents + A/B/C/D off the **v3.5.3** docs (A/B were the open NARROW angles; C/D re-confirm only their touched deltas). Reviewers write to `v3_5_3_GAP_REVIEW_*`. Clean/nits → minor-bump copy to `v3_6/` (`v3_6_0_*`) = the formal Agent-SDK A-Type gate kickoff.

## Round-3 review + fold APPLIED → v3.5.4 (round-4 pending)

**6 context-isolated subagents run off v3.5.3** (2 breadth + A/B/C/D). **Verdicts:** C = READY; A/B/D + both breadth = NARROW. Findings stand in `v3_5_3_GAP_REVIEW_{A,B,C,D,BREADTH_JOURNEY,BREADTH_INTEGRATION}.md`. Non-monotonic convergence in action — two of the round-3 findings were **self-inflicted by the round-2 folds**, exactly why the loop re-runs after every change:
- **[LOAD-BEARING · B-1] `previewToken` out of scope** — my round-2 A2-5 fold put `!previewToken` inside `populateStickyCard`, but that's a top-level function and `previewToken` is scoped to the init handler (`product.js:19`) → a ReferenceError on **every PDP load** (aborts hero/gallery/price/buttons). Fixed: §6.5b now **re-derives `previewToken` locally**. §4.5.d intentionally stays un-guarded (a previewed draft SHOULD show struck).
- **[LOAD-BEARING · A-D4-CSS] orphaned badge CSS** — I speced `.badge-unique` in DESIGN §D.4 but **no phase wrote it to `styles.css`** → the badge would render unstyled. Fixed: new **§9.2a** writes the rule (solid storefront tokens, no `color-mix()` → kills D3-2 too).
- **[D3-1] badge overlap** — `.card__media .badge` pins every badge to the same corner (no offset rule), so the round-2 "show on all `!sold`" default overlapped Featured. Fixed: gate the badge **`!sold && !p.featured`** (at most one of {Sold, Featured, Unique} per tile — zero new CSS); DESIGN §D.4 carries the optional badge-stack rule for "show on featured too."
- **Small:** `main.js` §4.3.b + §4.7.0 share one `DOMContentLoaded` anchor → coordination entry (A + B-2 + breadth-int, 3× confirmed) · §2.4 Schedule gate keys off **field-completeness**, not a preview step, with a fallback (J2 + A) · **legacy sold-row backfill** preflight in TESTING — a pre-v3.3 `available:false` row with stale non-null quantity would render buyable+struck under the quantity-primary rule (still 410s at checkout; Angle C) · J1 (item 30 homepage clause narrowed) · B-3 (§4.5.d non-guard noted) · A wording (§9.1 no-code-edit, §4.6 `code`-source). Ledger gained 34b–36b.

Living docs renamed `v3_5_3_*` → `v3_5_4_*`. **Round-4 (next):** scoped **A/B/D** re-run on the round-3 fixes (C stays READY — no fix landed in its lane) + optionally the 2 breadth. Reviewers write to `v3_5_4_GAP_REVIEW_*`. Clean/nits → minor-bump copy to `v3_6/` = the formal Agent-SDK A-Type gate kickoff.

## Round-4 verification → CONVERGED (v3.5.5) → PROMOTED to v3_6/ (A-Type gate ready)

**Round-4: 5 context-isolated subagents** (A/B/D scoped + 2 breadth) off v3.5.4. **Verdicts: A/B + both breadth READY/PASS, D one low NARROW, C closed-READY — NO load-bearing findings** (the loop's exit condition met). Breadth confirmed the round-3 crash fix (B-1) + CSS wiring (A-D4-CSS) hold; journey verified `out/products-app.js:47 readiness()` is field-only + reusable. Findings in `v3_5_5_GAP_REVIEW_{A,B,D,BREADTH_JOURNEY,BREADTH_INTEGRATION}.md` (note: written before the v3.5.5 rename, so they carry the v3_5_4-era names in-file — the trail, not living docs).

**v3.5.5 folded the two remaining nits** (each resolving multiple round-4 findings):
- **Card badges now MUTUALLY EXCLUSIVE by gate** — §6.5a/§6.3d: Sold (`sold`) / Featured (`!sold && p.featured`) / "One of a kind" (`!sold && !p.featured`) → exactly one per tile. Closes D4-R4-1 (homepage), retires the pre-existing Sold+Featured overlap (both breadth flags), makes item 30 structural.
- **§2.4 Schedule gate** reuses the repo-verified field-only `readiness()`; fallback is a concrete inline field-check (closes A's flag + J2).

**PROMOTED to `assets/docs/archive/v3_6/`** (`v3_6_0_{IMPLEMENT,ADDENDUM_DESIGN,ADDENDUM_TESTING,REVIEW_PROMPTS}.md`) — the converged v3.5.5 copied (not moved) + headers bumped to **v3.6.0** + a directory banner in each (source material — `design-handoff/`, ROADMAP, GPT files — stays in the sibling `v3_5/`; all relative paths resolve to `v3_5/`). This `v3_6_0_*` set is the **formal Agent-SDK A-Type gate kickoff**. The frozen `v3_5_5_*` + the full rounds-1–4 `GAP_REVIEW_*` trail remain in `v3_5/`.

**Convergence cost (calibration, not a bar):** 3 in-session fold cycles (rounds 2→3→4) + 17 subagent reviews after my own round-1. Two of the most-severe finds (the PDP `previewToken` crash, the orphaned badge CSS) were self-inflicted by earlier folds — the non-monotonic loop caught its own regressions. Commits: `9a4b87e` (v3.5.3) · `0158c38` (v3.5.4) · `2b37431` (v3.5.5) · this (v3.6.0 promotion), all on `dev`.

## Handoff — the formal A-Type gate (next)

The `v3_6_0_*` package is ready for the formal Agent-SDK fresh-instance A-Type gate (Sean's SDK flow). Per the versioning scheme: A-Type folds bump within `v3_6/`; B/C/D-Type → `v3_7/`; the `v4.0.0` execution copy → `v4_0/` after the architecture-removal consolidation. The one delta since the last full in-session round is the Featured-`!sold` gate (strictly simplifying) — the cold A-Type read is its net.

## Open threads for next session

1. Finish the v3.5.2 tail (G1/G3/G4/G6/G7/G8 + version bump) via a fold agent.
2. Then the package is ready for the **formal Agent-SDK gap-review gate** (minor-bumps into a new `v3_6/` for the A-Type gate per the versioning scheme; `v3_7/` for B/C/D; `v4_0/` = the execution copy after the architecture-removal consolidation). The SDK flow itself was still being (re)built by its author as of this session.
3. Genuine open decisions still carried as recommended-defaults (confirm at the gate): poster single-per-product; reconciliation alert → existing order-notify address; blink/badge split; series-taxonomy reconcile-at-build. The one runtime item is the #219 `applyPromotionCode()` probe on the dev preview (gates WS4 auto-apply; documented fallback exists).
