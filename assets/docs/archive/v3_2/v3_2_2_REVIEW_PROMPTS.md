# v3.2.2 — Gap-review prompts (B/C/D **round 3**; A-gate CLOSED at round 7)

**The build under review** = `assets/docs/archive/v3_2/v3_2_2_IMPLEMENT.md` (functional **WS1-3 + WS6 inventory** byte-anchored; **WS4-5** executable design) **plus its same-version addenda** `v3_2_2_ADDENDUM_DESIGN.md` (presentation: WS4 admin redesign + WS5 homepage) + `v3_2_2_ADDENDUM_TESTING.md` (verification). **The three are one build; every addendum is always in scope.** Effort: maximum. **A new instance per pass** (no context contamination). Reviewers change **nothing** — output is findings only, written to the named `GAP_REVIEW` file.

**Why B/C/D (not another A):** seven cold out-of-repo Angle-A rounds reached **READY TO BUILD** (round 7, `assets/docs/archive/v3_1/v3_1_7_GAP_REVIEW_A.md`) — the A-gate is **closed**. v3.2.2 carries a substantial design surface (a full /admin re-skin — token system + P0–P7 — and a homepage Lottie title + old-film hero), so **D (design-correctness) keeps its own lens** alongside B (fidelity) + C (integration) (per DEV_RULES, D is conditional on substantial design/UX). Collapse to B/C only if you'd rather — but then neither may skip the design addendum.

**Sequencing:** the A-gate is **closed** (7 rounds → READY TO BUILD). **Round 1 of B/C/D (vs v3.2.0) → all three NEEDS ANOTHER PASS (narrow)** — two real defects + precision/a11y folds; 0 rejects → v3.2.1. **Round 2 (vs v3.2.1) → B + C BOTH READY TO BUILD; D NEEDS ANOTHER PASS (narrow)** — D's one real columns-bug (the structured media editor's inline grid overflowed at a phone width, un-reachable by the breakpoint) + a token-mapping / legend / Upload-heading cluster, and C's one template-safety fix (the WS6 cutover `UPDATE` auto-ran on `db push`); 0 rejects, 0 false alarms — all folded → v3.2.2. This is **round 3**, run as **two different jobs** (the gate now closes **angle-by-angle**): an angle that returned **READY** re-runs **only with a named reason** — a fold that landed in *its* lane — and is then **SCOPED to that delta**, told it passed and to confirm just that, never a fresh full hunt (a passed angle at max effort will always find one more "polish" nicety; reflexively folding those manufactures work + new seams). An angle still **OPEN** gets a normal full re-review. So: **D** (returned NEEDS-ANOTHER-PASS) → full design-correctness re-review: confirm its round-2 findings folded clean + nothing new load-bearing. **B and C** (both READY on v3.2.1) each have a named X — and since **the repo is UNCHANGED** (these were docs-only folds), the only thing that can have moved in their lane is the v3.2.2 edits: **B** byte-checks the four new fold edits (the auth-comment CURRENT/NEW, the `min_amount` line, the commented migration SQL, the `.media-row__head` swap); **C** confirms the migration-mechanism change + the coupon readback path compose. **Their charters below are rewritten to scope them to that delta** — they're told they passed and to confirm X, NOT to re-audit the cleared build. **If a later round's folds touch no lane, that angle stays CLOSED** (no re-run for completeness' sake); the 2-subagent breadth pass is the cross-lane backstop for a fold whose consequence we didn't predict. Convergence is non-monotonic. Fold all findings → bump (`v3_2_2` → `v3_2_3` …) → regenerate this file (the shared ledger is **current-only**: supersede/replace an entry, don't append a contradiction) → re-run an angle only if it found something load-bearing. An **optional final cold-A holistic sanity pass** on the B/C/D-clean docs is welcome (→ `v3_2_2_GAP_REVIEW_A.md`). All clean → Sean approves → a fresh agent executes on a `feat/` branch off `dev`.

**What to hand each reviewer:** the **whole repo + the three v3.2.2 docs + THIS file** (B/C/D run in Claude Code at 1M context, so the file fits — and the shared **Settled — do not re-raise** ledger above must be in context). **C** also reads `assets/docs/EVERLASTINGS_STORE.md` first; **D** also reads the design addendum's research sources + the `FEEDBACK_ADMIN_v2_1_0.md` screenshots. The per-angle lens lives in each block below; the landmine ledger is **shared once above** (no longer re-inlined per block — that was for the A-loop's web-UI paste). The optional final cold-A pass gets the **three docs only, NO repo** — its lack of a repo is the point.

---

## The settled base — don't re-litigate what's shipped

The current store — everything described in `EVERLASTINGS_STORE.md` and present in the repo today — is **built, tested, and approved to go live**; it's the fixed, proven substrate. **v3.2.2 is a delta layered on top** (refunds, coupons-in-admin, chat-attach upload, admin polish, homepage). Reviewers judge the **v3.2.2 changes** and whether they **fit** that base — they do **not** redesign or re-flag settled/shipped behavior, and a finding must be about what *this build* adds/changes (or a real conflict it creates with the base). This is inlined into every block so reviewers don't manufacture blockers out of proven code.

## ⭐ The review lens (in every block — both parts)

**(a) North Star / thesis:** minimize Em's friction to manage her store — **every management capability must be doable in BOTH the /admin panel AND her Custom GPT, equally** (we can't rely on the GPT always being there). The GPT should do what a capable agent (Claude Code + skills/MCPs) could. A capability that reads "covered" in the docs but that Em can't actually trigger (in either surface) is a **real gap**, not a nitpick.

**(b) Broader mandate — the lens must NOT shrink the scope:** the North Star is the primary *functionality* lens, but it is **not** the filter for what counts as a gap. Across the **whole build (IMPLEMENT + BOTH addenda), every element**, also catch anything not truly **exclusively-executable** (the builder would have to discover/decide), any **unvalidated assumption**, and any **design-correctness** failure (the "columns-bug" class: a spec that *applies* cleanly but *renders wrong/incomplete*). Do not neglect the design addendum because the functionality lens feels primary — that exact mistake dropped the design review in v1.6's first round.

**(c) Read in full, co-design (don't just audit), flag-don't-assert.** Read ALL provided docs **end-to-end** — don't ration tokens (you're at maximum effort and context is managed for you; the four docs + STORE total ~48k tokens; grep/jump reading produced real mis-diagnoses last round). Think comprehensively: within the v3.2.2 delta, **a gap is a gap whether it's a flaw in what we wrote OR something the delta should address but omitted** — don't assume the build is mostly complete and you're only auditing what's present (distinct from the settled base: don't redesign the shipped substrate, but DO surface what the delta itself should cover). And you often can't see runtime/code behavior — when a finding depends on it, **FLAG it as needs-verification, do NOT assert it as broken** (in prior rounds several "broken" findings were contradicted by the actual code — round-2's "critical data-corruption", and round-3's "an undeclared response field is invisible" (the coupon ops are opaque — description-only 200s — so the GPT reads raw JSON), "the chat-attach branch is unauthenticated" (auth runs at upload.ts POST top), and "the hero CSS tokens are missing" (they exist) — though note the round-3 multi-cart over-refund WAS real, so flag-don't-assert cuts both ways — and round-5 added THREE more code-contradicted false alarms: "#222 must be body text" (it's the accent-button + active-subtab background), "homepage.js may load synchronously" (it's `defer`), ".hero h1 may be a direct-child selector" (it's a descendant) — all now verified and recorded in the docs, so for a delta on a settled base prefer "I can't verify X from the docs" over "X is broken").

## Settled — do not re-raise (the shared ledger; validate against reality, not training data)

> **Current-only + bounded.** This ledger accrues as findings fold (the 7 cold-A rounds + B/C/D rounds 1-2 so far → entries 1-61). A later fold that *supersedes* an entry **replaces** it (we never append a contradiction); entries are pruned at each condense. Every entry is a **verified finding from a prior round** — validate it against the repo, FLAG-don't-assert, and do **NOT** re-raise it as a new finding. **Shared once here, not re-inlined per angle** (the A-loop's web-UI agent needed a self-contained paste; B/C/D run in Claude Code with this whole file in context). Per DEV_RULES: keep this a single shared block.

1. **Vercel `api/*.ts` must compile to CommonJS** (the project `tsconfig`); ES-module output crashes the deployed runtime and **only surfaces on a real preview URL** (masked by `vercel dev`). The new `orders.ts` `POST` + the `upload.ts` changes must stay tsc-clean AND CJS-safe.
2. **No new Vercel function** — refund folds into `api/orders.ts` (new `POST` + a `vercel.json` rewrite); chat-attach folds into `api/upload.ts` (a rewrite). `ls api/*.ts` count unchanged (Hobby cap 11/12).
3. **`is_test` is never user-editable;** every new read/write stays scoped to `isTest` (`api/_lib/env.ts`). The refund order-lookup is `is_test`-scoped; Stripe's secret key is env-scoped (test vs live).
4. **The frontend derives an image's ROLE from its filename prefix** — `product.js:415` (`/\/(?:test_)?gallery-/`) + `:576` (`hero-`). There is **no stored role field**: the server names files `{role}-{slug}.{ext}` from the `role` param, and the page reads role back off that name. So passing the **correct role** is load-bearing; a wrong/missing role silently mis-categorizes or drops an image. Nobody renames a file.
5. **`openaiFileIdRefs` is documented but finicky** (blank arrays / 422s in the wild) — the by-link `uploadImage` is the **deliberate backstop** and must stay. `files.oaiusercontent.com` is public https → passes the existing `isPublicHttpUrl`; `download_link`s expire ~5 min; ≤10 files/call.
6. **The GPT Instructions have an ~8k-char cap** — the v3.1 edits (refund flip, attach-first, alt-text, coupon read-back, poster aside) must fit; flag if the NEW text plausibly blows the budget.
7. **Refund is AMOUNT-BASED** against the PaymentIntent (Stripe refunds aren't line-item-aware), and **one cart = N sibling `orders` rows** sharing one `stripe_payment_intent`. `refundOrder` refunds `amount_cents` (default = the clicked order's line amount) and flips/relists **only** the marked `relist_product_ids` — never the whole cart. `idempotencyKey = refund-<pi>-<amount>-<ids>` (a double-click is idempotent; a different piece/amount on the same PI isn't blocked). The `charge.refunded` webhook (`api/webhook.ts:60-89`) flips `orders.status='refunded'` on a **full-PI** refund (idempotent with the handler's per-order flip); an intermediate partial doesn't fire, so the handler owns that piece's flip. Don't introduce a double-write hazard, and don't re-flag the per-order button as over-refunding (round-3 headline — fixed).
8. **Refund never auto-relists** (a damaged-item refund mustn't silently re-list). Relist is a separate, **state-aware**, confirmed step (`available:true` if published-but-sold; `unarchive` if archived). **The refund's `products(...)` Supabase embed must deserialize to a to-one OBJECT** (read like the GET at `orders.ts:65`/`:172`) — if it's wrong the refund still succeeds but `relist` is malformed and the "put it back up for sale?" prompt **silently never fires** (a parity hole `tsc` won't catch — verify against a real response).
9. **The /admin upload control already uses the SAME `/api/upload` pipeline** (multipart → Cloudinary per-role crop → R2/CDN). The authorized admin `GET /api/products` returns **full** rows, so `stripe_product_id` / `is_published` / `archived_at` are present for the coupon scope picker + product-state filter.
10. **Coupons: never make the GPT decode a raw Unix timestamp** (the `FEEDBACK_COUPON_v2_1_0` regression — it misread July for June). The backend returns `expires_display` (human, store TZ America/New_York) **alongside** `expires_at`; the GPT relays that. `product_ids` are **`stripe_product_id`** (NOT the Supabase id); a product-scoped coupon needs a **published** product.
11. **The structured admin media editor must emit the SAME shape** `product.js`'s `populateMedia` reads — `{ type:'video'|'youtube', url, autoplay?, loop?, poster?, alt? }` — so `api/products` + `product.js` stay untouched; MP4s render before YouTube; empty media hides the section.
12. **`cleanUrls: true`** — `vercel.json` rewrite *destinations* must drop `.html` (silent 404s otherwise). The two new rewrites are API→API (no `.html`), but confirm.
13. **Storefront brand is untouched.** /admin gets a **neutral/template** aesthetic (the addendum's `:root` tokens) — NOT the Everlastings plum/lavender/serif, and not anchored to august.style tokens. The token literal-sweep must not leak storefront brand into /admin (or vice-versa).
14. **Reduced-motion + a11y preserved.** The hero's `prefers-reduced-motion` fallback stays — it lives at `styles.css:998` (`.hero__glow::after{animation:none}`) + the inline poster swap at `index.html:379`, **NOT** `styles.css:376` (an earlier hint that had drifted; B round-2 #1, no build impact since WS5's `styles.css` edits are locate-by-content per ledger 17). The new Lottie title is `aria-hidden` decoration over a **real `<h1>`** (SEO + screen-reader + no-JS + reduced-motion fallback to the static title).
15. **The hero CDN objects are served `immutable, max-age=1yr`** — the old-film hero swap needs a **versioned key** (e.g. `homepage-hero-animation-v2.mp4`) + URL edits, never an in-place overwrite (cache won't bust).
16. **The go-live (`main`) version is untouched** by v3.2.2 — it's a separate `dev` cycle that ships on its own later. Don't entangle them.
17. **WS4/WS5 are spec'd as executable DESIGN (concrete default + render-tune), not byte-anchored like WS1–3.** Judge the design addendum by "concrete enough that the builder never guesses + Sean can render-tune on the live preview," not by final-pixel values. Real content (the Lottie JSON, the HyperFrames render) is a content-creation step, never a build/test gate. The two remaining mechanical bits flagged in WS4 (the token literal-sweep + P0's product-list state-filter JS / back-nav) must be concrete enough to apply without deciding.
18. **Inventory lives ONLY in Supabase `products.quantity`; Stripe holds NO stock** (the Checkout line-item `quantity` is transactional, forgotten after charging). **WS6** makes a sale **decrement** `products.quantity` (the atomic `record_sale` RPC) + set `available=(quantity>0)`; it never touches Stripe and **never archives**. Don't assume a sale zeroes quantity, deactivates a Price, or that Stripe tracks stock.
19. **Refund relist restores BOTH axes (WS6.3):** unarchive when archived AND `quantity+1` (which re-flips `available` true) — not XOR; a relisted piece must be genuinely **buyable** again.
20. **The assembled GPT instructions must stay < 8000 chars** after all WS1-3 edits (the static gate checks it) — over the cap the GPT silently truncates its instructions.
21. **ONE canonical `productState(p)`** (from `is_published`/`draft`/`available`/`archived_at`) drives BOTH the P0 filter tabs AND the P2 status badge — no divergent predicates.
22. **The architecture doc was just reconciled, but a doc line can still lag the code** (last round tripped on a stale ":678 quantity=0" line the webhook never did) — verify behavior against the build guide + code, flag-don't-assert, and treat the doc's Status/Version header as suspect first.
23. **Full-capability parity — both directions, no single point of failure (Sean's standing rule).** /admin must do EVERYTHING the GPT can AND vice-versa; never leave the owner dependent on the GPT (it can be down). Design for the reusable-template **"User"** with a large catalog, NOT Emy's tiny one — optimizing for "how Emy will use it" is exactly what made us double back before (e.g. by-link share vs. a real upload). A capability present in one surface but not the other is a **real gap** (this round closed the coupon multi-product scope in /admin to match the GPT).
24. **The cart is one-of-each-per-order.** It dedupes by `product_id` (`main.js:121` returns early on a re-add), there is no quantity selector, and each checkout line-item is `quantity:1` — so `metadata.items` / `productIds` / `record_sale`'s `p_ids` **never contain a duplicate id**. A qty-N piece sells via N **separate sequential orders**, not N-in-one-cart. Don't read `record_sale`'s per-row decrement as an under-count bug (the round-2 headline finding did — it can't fire under this cart).
25. **The refund relist prompt ALWAYS offers now** (Sean's call — minimize friction, never strand stock): the wording branches on current state — a **down** piece (sold-out or archived) → "re-list + make available again", an **in-stock multi-qty** piece → "+1 to its available quantity"; both restore the returned unit (`quantity+1`, unarchive if archived). It still never *silently* relists — it asks.
26. **One PaymentIntent spans N sibling `orders` rows** (`webhook.ts:185` writes one row per product) — a refund is an **amount** against the PI, not per-row. The round-3 build's amount-based composer passes `amount_cents` + `relist_product_ids` so it refunds/relists only the **marked** pieces (the per-order Refund button used to refund the whole cart — that was the round-3 headline, now fixed). Don't re-flag the button as over-refunding, and don't mis-read the one-PI/N-orders fan-out as a bug.
27. **The coupon Action responses are OPAQUE** — `listCoupons`/`createCoupon` declare a 200 with a *description only* (no `schema.properties`), so the GPT receives the **raw JSON** and a new wire field (e.g. `expires_display`) is visible **without** a schema edit. (Contrast `refundOrder`, which DOES enumerate its `relist` response — additions there must be declared.) Don't flag "undeclared response field is invisible" for the opaque coupon ops.
28. **Auth is at the top of `upload.ts` POST** (`:118`) — `authorize(request)` runs before the JSON content-type fork that reaches `handleAttachedRefs`, so the chat-attach branch is already gated. Don't flag it as unauthenticated.
29. **The GPT-instruction file was FULLY RESTRUCTURED (round-4 #1) to fit the hard 8000 cap — Phase 3.9, verified 7732/8000** (round-7 reworded only the relist sentence for an unambiguous parse, +3 chars; the build re-counts the shipped `.txt`). Review the instructions against **Phase 3.9's final text**, NOT the per-section 1.4/2.3/3.5 CURRENT→NEW blocks (those document the rule deltas; the file ships wholesale from 3.9). The cap is HARD — any instruction add must re-verify `wc -c`. Don't re-flag the cap as unmet (it's closed) or critique the superseded dense wording.
30. **Round-4 fixes already landed — DON'T re-flag:** `endOfDayET` uses `formatToParts`/`shortOffset` (not a brittle `new Date(localeString)` round-trip); the WS6 cutover update is scoped to actually-sold rows (`exists (orders)` — won't zero a paused piece's stock); every `.pill` state has concrete CSS incl. `.pill.refunded`; `/admin` upload nudges alt text (GPT-parity); the Lottie init has a `data_failed` + SVG-content guard (no blank hero). STORE's pre-WS6 "no decrement" lines (`:614`/`:709`) are correct **shipped** truth synced at the as-built Phase 6.4 — a mid-build STORE edit was proposed and **rejected** (no-mixed-truth); don't re-flag it as a contradiction. The /admin refund panel fetches a cart's **full** sibling set via `GET /api/orders?payment_intent=` (round-4 breadth fix, Phase 1.1c — NOT the active-subtab slice), so don't flag it as under-listing a partially-shipped multi-piece cart.
31. **The GPT schema file has a per-`summary`/`description` 300-char cap** (the static gate checks `every summary < 300`) — as hard as the 8000 instruction cap. COUNT every new/edited schema string; don't estimate (round-5 #1 caught a `refundOrder` summary at **314** the doc had estimated "≈295" — it would have failed the gate on minute one).
32. **Attached VIDEO routes by-link only** (`uploadImage`), never `uploadImages` — enforced in BOTH the MEDIA instruction AND a server guard in `handleAttachedRefs` (rejects `video/*` with a "send it as a link" failure). An attached video would otherwise get an image role + land in `images[]` and break the page (`populateMedia` reads `media[]`, not `images[]`). Round-5 #2 — don't re-flag.
33. **Round-5 verified-against-code, now recorded — DON'T re-raise as risks:** the two `#222` literals are accent backgrounds (→`--c-accent`; body text is already `--c-text`); `homepage.js` is loaded `defer` (so the Lottie defer-ordering holds); `.hero h1` is a **descendant** selector (so the `.hero__title` wrap keeps it matching).
34. **The GPT ATTACH path now exposes all image roles** (round-6 #3) — `uploadImages` `roles[]` (schema 3.4a) + Phase 3.9 step 2 enumerate `hero, gallery-NN, detail-NN, thumbnail, seo_thumbnail, checkout_image` (matching the 7-zone /admin upload + the by-link `uploadImage`). Don't re-flag the attach path as a 4-role parity hole; video stays by-link only (landmine 32).
35. **`handleAttachedRefs` guards role collisions** (round-6 #4) — a `usedRoles` Set rejects a duplicate/colliding role with a per-file failure (the role is claimed only on a successful upload), so two files can't silently overwrite each other in R2. Don't re-raise the silent-R2-overwrite path.
36. **WS4 init-wiring is ONE consolidated CURRENT/NEW block** (round-6 #1) — `attachEventListeners` (DESIGN P0(iii)) shows all four deltas (remove `#upload-btn` + dead `onUploadImage`; add `wireUploadZones`/`wireProductSubtabs`/`editor-back`; scope the orders subtabs) in one diff; **`.upload-zones`/`.upload-zone`/`.zone-msg` ship concrete-default CSS** (round-6 #2) + a P5 single-column override. Don't re-flag WS4 wiring as prose-only or the zones as CSS-less.
37. **Round-6 verified-against-code, now recorded — DON'T re-raise:** the **All Orders** orders subtab EXISTS (`admin/index.html:247`, `data-status="all"`); `productState` returns exactly 5 states (no `available` — the orphan `.pill.available` rule was dropped); the WS6 migration `20260616000001` is clear of the latest existing `20260605000001`; `new File([bytes])`/`Buffer` are Node-20 globals validated at runtime by TESTING item 14; the refund response `status` mirrors the actual flip (`'refunded'` only when pieces flipped, else the order's unchanged status).
38. **F1 fixed — the `wireCouponEvents()` phantom is gone (round-7 #1).** DESIGN P0(iii)'s parenthetical now correctly says **Phase 2.1e wires the coupon form/refresh/search at its OWN anchor (`admin.js:164-170`)** via three individual `addEventListener` calls — there is **no** `wireCouponEvents()` fn, and 2.1e is **not** in the P0(iii) consolidated block. Don't re-raise the doc contradiction.
39. **F2/F15 — both upload-op summaries verified < 300 (round-7).** `uploadImage` summary rewritten to **250** chars (redirects attached photos → `uploadImages`, covers by-link + video); `uploadImages` summary is **273** (already redirects video → `uploadImage`). Both counted, both under the cap (landmine 31). Don't re-flag either as overflowing.
40. **F5 — the Node-20 runtime is pinned (round-7).** `package.json` pins `engines.node` `">=20"` and there's no `vercel.json` runtime override, so `new File`/`Buffer` in `handleAttachedRefs` are present (the static gate greps both). Don't re-flag the Node-20 dependency as unpinned / silent-on-Vercel-default.
41. **F7/F13 — the /admin upload zones are now guarded (round-7).** Single-role zones (hero/thumbnail/seo_thumbnail/checkout_image) **confirm-then-replace** instead of silently overwriting the R2 object + appending a dupe row (symmetric with `handleAttachedRefs`' `usedRoles`, landmine 35); a **client-side MIME guard** keeps video out of image zones and images out of the Video zone (the server's `ALLOWED_MIME` accepts both, so this is the only client gate). Don't re-flag /admin upload as more-dangerous-than-the-GPT, or the Video zone as MIME-unguarded.
42. **F11 verified — the product-state filter is CLIENT-SIDE, NOT a parity gap (round-7).** P0's All/Live/Draft/Sold/Archived subtabs filter the already-loaded list via `productState(p)`; the GPT filters `listProducts` results identically. Neither surface has a *server-side* state filter, so adding a `listProducts` `state` schema param would make the GPT MORE capable than /admin (reverse asymmetry), not close a gap. Don't raise "listProducts needs a state filter" as a parity hole.
43. **F16/F14/F6 landed (round-7).** The structured MP4 editor now has a **`controls` toggle** (GIF-like "no buttons" vs click-to-play) → /admin↔GPT video parity is complete and TESTING item 21 no longer needs a Supabase-Studio seed (F16); `formatExpiry`/`endOfDayET` read a single `STORE_TIMEZONE` constant, not a literal (F14); the refund button reads **"Refund this purchase…"** (F6). Don't re-flag any as missing.
44. **F10 DEFERRED — simultaneous multi-admin is OUT OF SCOPE, period (round-7, Sean's call).** The store is designed for **one operator at a time**; the relist step's `quantity = refund-time-quantity + 1` could only go stale under a *concurrent* edit in another tab/seat, which can't happen single-operator. An atomic `record_relist` RPC is the clean follow-up IF multi-seat ever ships — not a v3.2 gap. Don't raise the relist TOCTOU (or any "two admins at once" race) as a build blocker.
45. **B2/C3 — `processOne`'s extraction range is `:195–320` (B/C/D round 1).** The `try` opens at `upload.ts:229` and its `catch` (whose `return jsonResponse(…, { error:'Upload failed' }, 500)` is one of the swapped error-returns) closes at `:320`, *after* the success return at `:316` — so the whole `try…catch` moves into `processOne` intact. Phase 3.1 now cites `:195–320`. Don't re-flag the range as ending at `:316` / orphaning the catch.
46. **B3/C4 — `engines.node ">=20"` is byte-anchored (round 1).** Phase 3.2b is a real CURRENT→NEW `package.json` edit (insert `"engines": { "node": ">=20" }` beside `dependencies`), not just prose + the gate. Don't re-flag it as the one prose-only / gate-only edit.
47. **B1/C2/D8 — the `add-media-row` wiring lives in DESIGN P0(iii)'s consolidated block, NOT a separate 3.7b `:152` edit (round 1).** Three reviewers flagged the interleave; the fix folds the one `$('add-media-row')…` line into P0(iii)'s NEW block so `admin.js:152-161` is touched exactly ONCE (at WS4). 3.7b defines `addMediaRow` + the button unwired and points at P0(iii). Don't re-raise the ordering trap or ask for a separate 3.7b wiring edit.
48. **C1 — `openEditor`'s `#upload-status` reset is REPOINTED to `.zone-msg` (round 1).** `openEditor`'s last line (`admin.js:322`) is a reference SEPARATE from the deleted `onUploadImage`; after P3d removes `#upload-status` it now clears `.upload-zone .zone-msg` instead — else `$('upload-status')` was `null` and threw a `TypeError` on **every** editor open (runtime-only, tsc/`node --check`-invisible). The static gate greps `upload-status`→0. Don't re-flag the dead reference.
49. **D1 — the Lottie title hide is `color:transparent` on the IN-FLOW `<h1>`, NOT the absolute-clip idiom (round 1).** The old `.has-lottie` rule took the `<h1>` out of flow; since the Lottie div is also `position:absolute`, the wrapper collapsed to height 0 and the write-on rendered invisible **in its success path** (fallbacks all looked fine). `color:transparent` keeps the `<h1>` in flow (box height + a11y/SEO) while the absolute Lottie overlays it. Don't re-flag the zero-height collapse or re-propose the clip idiom.
50. **D2/D5 — `renderProductList` is BYTE-ANCHORED (round 1).** A CURRENT→NEW block (DESIGN P2) replaces the shipped two-pill code (`.pill.unsent`-for-sold + `.pill.shipped`-for-live) with ONE `.pc-badge` from `productState`, the `is-archived` dim, quiet `.pc-meta`, and a **branched empty copy** ("No products yet" vs "No products match this filter" — products can be unfiltered, so it branches, unlike `renderOrders`' always-filtered single message). Don't re-flag it as prose-only-over-contradicting-code or the empty state as ambiguous.
51. **D4 — admin.js inline styles use the `var(--c-token, #hex)` fallback form; TESTING item 24 bans only BARE hex (round 1).** The WS3.7 template-string styles (`addImageRow`/`updateCoverage`/`addMediaRow` + the `.img-thumb` rule) carry tokens-with-fallback so they render before §4.1's `:root` lands; the only hex left anywhere is inside such fallbacks. Item 24's grep targets *bare* hex only. Don't re-flag admin.js raw hex, or item 24 as contradicting the sweep, or `updateCoverage`'s old literal `--c-success`/`--c-warn` duplication (gone).
52. **D6/D7 — admin a11y + contrast folded (round 1).** Upload-zone file inputs are wrapped INSIDE their `<label>` (clickable + associated); the structured media inputs (`.m-url`/`.m-poster`/`.m-alt`) carry `aria-label`s; `.empty` uses `--c-text-muted` (≈5.8:1, AA) not `--c-text-faint` (reserved for true placeholders). Don't re-raise unlabeled zone inputs / placeholder-only media inputs / empty-state contrast.
53. **D3/D9/D10 — mobile + legend + pill folded (round 1).** P5's `.img-url-row` stacks to a single `1fr` column at ≤640px (the 5-cell row no longer crushes URL+Remove into the 40px thumb column; `.img-thumb` keeps its own 40px); the state-legend has an insertion anchor (after `#product-subtabs`, before `#products-list`); P2's `.pill` keeps `display:inline-block` (additive merge into the existing rule). Don't re-raise the mobile mis-stack, the legend's missing anchor, or the dropped inline-block.
54. **D-R2-1 — the structured media editor's clip head row is a CLASS, `.media-row__head`, stacked in P5 (round 2).** Its desktop `110px 1fr auto` grid moved from an inline `style=` (which a media query can't override — inline wins the cascade, so it overflowed at ~360px) into a real inline-`<style>` rule, added to P5's `≤640px` single-column stack beside `.img-url-row`; `.m-url` got `min-width:0` and the three media inputs use `--c-border-strong` (the same `var(--token,#hex)` fallback form). Don't re-flag the media editor as mobile-overflowing or its grid as un-overridable.
55. **C-R2-1 — the WS6 cutover `UPDATE` ships COMMENTED (round 2).** Phase 6.1's destructive `quantity=0` cutover is commented out, so `supabase db push` applies ONLY the safe `record_sale` function; the operator runs the SELECT, confirms, then uncomments + runs the UPDATE by hand, once. (A doc-only "eyeball-first" gate couldn't bind a template "User" with a multi-stock catalog — a blind apply would zero an intentionally-paused-but-sold piece.) Don't re-flag the cutover as auto-zeroing on `db push`.
56. **D-R2-2 — §4.1 names the link + retained-pill token targets (round 2).** The literal-sweep maps `a{#06c}`/`a:hover{#03f}` → `--c-accent`/`--c-accent-hover` and retokens the two KEPT order-email pills `.pill.shipped` → `--c-success-bg`/`--c-success` and `.pill.unsent` → `--c-danger-bg`/`--c-danger` **in place** (orders still use them, `admin.js:720-723`). Don't re-flag links or those two pills as untargeted by item 24, or propose deleting the kept pills.
57. **D-R2-3 — the product-state legend has NO `Refunded` swatch (round 2).** `productState` returns only live/edits/sold/draft/archived, so the legend (a key for the product grid) dropped the `Refunded` swatch; the `.pill.refunded` rule itself STAYS for the order-card pill. Don't re-flag the missing swatch as a gap or ask to restore it.
58. **C-R2-2 — the admin.js auth-model header comment now names `PRODUCT_API_KEY` (round 2).** The `:3-7` comment said `/api/orders` was JWT-only; it's corrected to "JWT **or** PRODUCT_API_KEY via requireAdmin" (the GPT drives refund/list/markShipped over `/api/orders`). Behavior was always correct. Don't re-flag the comment.
59. **C-R2-3 — the coupon list relays `min_amount`; readback is complete on BOTH surfaces (round 2).** `handleCouponList` adds `min_amount` (cents) to each listed coupon; `renderCoupons` shows `· min $NN.NN`; the GPT reads it off the opaque-200 raw body (no schema/instruction edit — ledger 27). So every settable coupon attribute (code/percent-or-amount/scope/expiry/cap/**minimum**) is verifiable in /admin AND chat. Don't re-flag `min_amount` as write-only.
60. **D-R2-4 — a short "Upload" section heading is kept above the seven zones (round 2).** P3d's NEW markup restores a `<strong>Upload</strong>` heading so the upload area still reads as a labeled section. Don't re-flag the zones as title-less.
61. **D-R2-5 — two render-tune nits folded (round 2).** `.pc-badge` uses `--s-3` (12px = the card padding, so it sits flush on the image, not 4px into the padding); the P4 skeleton image block is `height:140px` (matching the real `.product-card img`, so cards don't reflow when skeletons resolve). Don't re-raise the badge offset or the skeleton reflow.

---

## Angle A — cold / out-of-repo (self-containment + completeness) — ⛔ A-GATE CLOSED (round 7 → READY TO BUILD)

> Kept for reference + an **optional final holistic sanity pass** after B/C/D fold clean (three docs only, NO repo). Not part of the active B/C/D batch.

```
You are a senior engineer doing a pre-build gap review. Effort: maximum. Do NOT change anything — your only output is findings (write them to v3_2_2_GAP_REVIEW_A.md, or print the full file contents if you have no filesystem).

THE REVIEW LENS — judge every gap against BOTH, not against doc-internal consistency:
(a) North Star: minimize Em's friction to manage her store; EVERY management capability must be doable in BOTH the /admin panel AND her Custom GPT, equally (the GPT should do what a capable agent could). A capability that reads "covered" but that Em can't actually trigger in either surface is a REAL gap.
(b) Broader mandate (don't let the lens shrink the scope): the North Star is the primary functionality lens, NOT the filter for what counts as a gap. Across the WHOLE build (the IMPLEMENT + BOTH addenda — design + testing — every element), also catch anything not truly exclusively-executable, any unvalidated assumption, and any design-correctness failure (a spec that APPLIES cleanly but RENDERS wrong/incomplete). Do not neglect the design addendum.

READ IN FULL + CO-DESIGN + FLAG-DON'T-ASSERT: read ALL provided docs end-to-end — don't ration tokens (you're at max effort, context is managed for you; grep/jump reading caused real mis-diagnoses last round). Within the v3.2.2 delta a gap is a gap whether it's a flaw in what we wrote OR something the delta SHOULD address but omitted — don't assume the build is mostly complete and you're only auditing what's there. When a finding depends on runtime/code behavior you can't directly see, FLAG it as needs-verification — do NOT assert it as broken (in prior rounds several "broken" findings were contradicted by the actual code — round-2's "critical data-corruption", and round-3's "an undeclared response field is invisible" (the coupon ops are opaque — description-only 200s — so the GPT reads raw JSON), "the chat-attach branch is unauthenticated" (auth runs at upload.ts POST top), and "the hero CSS tokens are missing" (they exist) — though note the round-3 multi-cart over-refund WAS real, so flag-don't-assert cuts both ways — and round-5 added THREE more code-contradicted false alarms: "#222 must be body text" (it's the accent-button + active-subtab background), "homepage.js may load synchronously" (it's `defer`), ".hero h1 may be a direct-child selector" (it's a descendant) — all now verified and recorded in the docs, so for a delta on a settled base prefer "I can't verify X from the docs" over "X is broken").

SETTLED BASE — don't re-litigate what's shipped: the current store (all of EVERLASTINGS_STORE.md + the repo as it stands today) is built, tested, and approved to go live — the fixed, proven substrate. This build (v3.2.2) is a DELTA on top. Review the v3.2.2 changes for gaps + whether they FIT the base; do NOT redesign or flag settled/shipped behavior — a finding must be about what THIS build adds/changes, or a real conflict it creates with the base.

CONTEXT
- You are given FOUR documents and NO repository: v3_2_2_IMPLEMENT.md (the plan — WS1-3 byte-anchored with CURRENT/NEW; WS4-5 spec'd as executable design), v3_2_2_ADDENDUM_DESIGN.md (WS4 admin redesign + WS5 homepage), v3_2_2_ADDENDUM_TESTING.md (the E2E plan). A FRESH agent executes ALL of it against the everlastings repo, then tests on the dev preview. "Exclusively executable" = it embeds the exact current code + exact replacement for every edit, so the builder LOCATES and APPLIES, never DISCOVERS or DECIDES. EVERLASTINGS_STORE.md — the store's architecture/state doc, so you can judge whether the plan covers everything the store needs.
- LANDMINES (validate the plan against these, not training data):
  → LANDMINES = the **Settled — do not re-raise** ledger (entries 1-61) in the shared section above — you have this whole file in context. Validate each against the repo (verified findings from the 7 cold-A rounds + B/C/D rounds 1-2, not training-data guesses), FLAG-don't-assert, and do NOT re-raise any as a new finding. (High-frequency false-alarm classes: 7/8 refund amount+relist, 22/30/37 doc-lag+verified, 23 parity, 29/31 the char caps, 42 client-side filter, 44 multi-admin-deferred; round-1 folds 45-53; round-2 folds 54-61 — the `.media-row__head` class stacked in P5, the COMMENTED cutover UPDATE, links→`--c-accent` + pills-retoken-in-place, no `Refunded` legend swatch, the `PRODUCT_API_KEY` auth comment, coupon `min_amount` readback, the kept Upload heading, badge `--s-3` + skeleton `height:140px`.)

ANGLE A — cold / out-of-repo. Your lack of a repo is the point. Two jobs:
1. SELF-CONTAINMENT: find every place the builder would have to open a file, guess, recall a library's behavior, or make a decision the plan didn't make for it — across all three docs, including the design addendum.
2. COMPLETENESS / PARITY (the holistic pass, through the lens): can Em FULLY run the store — refund (+ state-aware relist that restores stock), coupons (create/list/end + human dates), photos by attach AND by link, MP4 config, edits, orders, stock/inventory (a sale decrements quantity, sells out only at 0, relist restores it) — in BOTH /admin AND the GPT? Cross-check every capability against both surfaces; flag any that's possible in one but not the other, or that reads covered but isn't actually drivable. Stress the design too: is the admin redesign + homepage spec complete enough to build, accessible, and honest about what's render-tune?

OUTPUT
- A gap list RANKED by how likely each is to derail the build or leave a capability missing: location (doc/section/phase), what's wrong/missing, the concrete fix.
- The single most important "if you fix one thing" insight.
- One-line verdict: READY TO BUILD or NEEDS ANOTHER PASS.
Be concrete: "WS1 returns relist.available but the GPT instruction never tells it to act on relist.archived" beats "refund flow looks thin."
```

---

## Angle B — fidelity (repo)

```
You are a senior engineer doing a pre-build gap review. Effort: maximum. Do NOT change code or docs — output findings only (write them to v3_2_2_GAP_REVIEW_B.md).

⚠️ ROUND-3 RE-RUN — SCOPED, NOT A FRESH REVIEW. You returned READY TO BUILD on v3.2.1: you already read the whole build end-to-end and cleared it on fidelity. We're asking you to look again for ONE reason — between v3.2.1 and v3.2.2 we folded a few edits, and these four landed in YOUR (fidelity) lane:
  (1) a new byte-anchored CURRENT/NEW correcting the admin.js:6-7 auth-model header comment (it now says /api/orders accepts a Supabase JWT OR PRODUCT_API_KEY);
  (2) min_amount added to handleCouponList's per-coupon push + a `· min $NN.NN` segment in renderCoupons;
  (3) the WS6 cutover `update products … set quantity = 0` in the Phase 6.1 migration is now COMMENTED out (record_sale stays live + uncommented);
  (4) the structured media editor's clip head row changed from an inline grid to class="media-row__head" (its base rule added in the DESIGN P3d concrete-CSS block; .m-url gained min-width:0).
The repo has NOT changed since your last pass (these were docs-only folds), so the ONLY thing that can have moved in your lane is those four edits. YOUR SCOPE THIS ROUND = byte-check exactly those four: do the NEW blocks quote the real CURRENT? do the new references resolve (e.g. pc.restrictions?.minimum_amount, centsToDollars)? is the commented SQL still a valid migration with record_sale intact? does the .media-row__head class have a real definition + no leftover inline grid? Do NOT re-audit the ~20 anchors you already cleared (the full ANGLE B charge below is your first-pass reference — treat the rest as cleared), and do NOT surface polish on parts you blessed — flag ONLY a genuine fidelity defect the v3.2.2 delta introduced. (The "read the whole build / don't shrink the scope" mandate below was for your FIRST pass — it exists so a reviewer doesn't wrongly EXCLUDE necessary parts of the build; you did that pass and cleared it, so this narrowed delta-scope is deliberate, not a violation.)

THE REVIEW LENS (both): (a) North Star — minimize Em's friction; every capability doable in BOTH /admin AND the GPT. (b) Broader mandate — the lens does NOT shrink the scope: search the WHOLE build (IMPLEMENT + BOTH addenda, every element) for anything not exclusively-executable, any unvalidated assumption, any design-correctness failure. Fidelity matters because a CURRENT block that no longer matches the tree means the builder DISCOVERS/DECIDES — breaking "exclusively executable."

READ IN FULL + CO-DESIGN + FLAG-DON'T-ASSERT: read ALL provided docs end-to-end — don't ration tokens (you're at max effort, context is managed for you; grep/jump reading caused real mis-diagnoses last round). Within the v3.2.2 delta a gap is a gap whether it's a flaw in what we wrote OR something the delta SHOULD address but omitted — don't assume the build is mostly complete and you're only auditing what's there. When a finding depends on runtime/code behavior you can't directly see, FLAG it as needs-verification — do NOT assert it as broken (in prior rounds several "broken" findings were contradicted by the actual code — round-2's "critical data-corruption", and round-3's "an undeclared response field is invisible" (the coupon ops are opaque — description-only 200s — so the GPT reads raw JSON), "the chat-attach branch is unauthenticated" (auth runs at upload.ts POST top), and "the hero CSS tokens are missing" (they exist) — though note the round-3 multi-cart over-refund WAS real, so flag-don't-assert cuts both ways — and round-5 added THREE more code-contradicted false alarms: "#222 must be body text" (it's the accent-button + active-subtab background), "homepage.js may load synchronously" (it's `defer`), ".hero h1 may be a direct-child selector" (it's a descendant) — all now verified and recorded in the docs, so for a delta on a settled base prefer "I can't verify X from the docs" over "X is broken").

SETTLED BASE — don't re-litigate what's shipped: the current store (all of EVERLASTINGS_STORE.md + the repo as it stands today) is built, tested, and approved to go live — the fixed, proven substrate. This build (v3.2.2) is a DELTA on top. Review the v3.2.2 changes for gaps + whether they FIT the base; do NOT redesign or flag settled/shipped behavior — a finding must be about what THIS build adds/changes, or a real conflict it creates with the base.

CONTEXT
- The build = v3_2_2_IMPLEMENT.md (WS1-3 byte-anchored) + v3_2_2_ADDENDUM_DESIGN.md (WS4 admin redesign + WS5 homepage — CURRENT/NEW + render-tuned defaults) + v3_2_2_ADDENDUM_TESTING.md. A FRESH agent applies ALL of it to THIS repo, then tests on the dev preview. Every code edit quotes a CURRENT block (locator) + a NEW block. Byte-check the design addendum's DECIDED blocks at the same bar; for render-tuned defaults judge "concrete enough the builder never guesses," not whether it's the final value.
- LANDMINES (validate against the repo, not training data):
  → LANDMINES = the **Settled — do not re-raise** ledger (entries 1-61) in the shared section above — you have this whole file in context. Validate each against the repo (verified findings from the 7 cold-A rounds + B/C/D rounds 1-2, not training-data guesses), FLAG-don't-assert, and do NOT re-raise any as a new finding. (High-frequency false-alarm classes: 7/8 refund amount+relist, 22/30/37 doc-lag+verified, 23 parity, 29/31 the char caps, 42 client-side filter, 44 multi-admin-deferred; round-1 folds 45-53; round-2 folds 54-61 — the `.media-row__head` class stacked in P5, the COMMENTED cutover UPDATE, links→`--c-accent` + pills-retoken-in-place, no `Refunded` legend swatch, the `PRODUCT_API_KEY` auth comment, coupon `min_amount` readback, the kept Upload heading, badge `--s-3` + skeleton `height:140px`.)

ANGLE B — fidelity. Open every file the plan edits and verify:
- Every CURRENT block matches the working tree BYTE-FOR-BYTE (whitespace, content). Line numbers are hints; the quoted text is the anchor — flag any CURRENT that no longer matches. Cover: orders.ts (imports :5-8, PATCH close :237-238); vercel.json :12 + :19; the schema markShipped tail + the uploadImage op :266-284; the GPT instructions :6/:19/:23/:25/:27; products.ts coupon handlers (:741 create return, :751-752, :786); upload.ts (:129-138 JSON intake, :195-320 tail [through the closing catch], the helpers); admin.js (:104-107 tabs are in index.html; :152, :196-211, :164-170, :298, :331-345, :371-372, :449-455, :770-771, :799-804, :830-832, :834-838); admin/index.html (:61, :159, :185-186, :256-257); product.js role regexes (:415, :576).
- Every NEW block applies cleanly + references only things that exist: imports resolve (the new `stripe` import in orders.ts; `formatExpiry`); the refund POST's `products(...)` embed reads to-one like the GET (LANDMINE 8); the upload `processOne` refactor preserves the :202-320 body verbatim (through the closing catch) with the return-swaps; `handleAttachedRefs`/`positionalRole`/`processOne` are module-level above POST; the new admin functions (openRefundPanel/submitRefund/relistPiece, loadCoupons/renderCoupons/onCreateCoupon/onDeactivateCoupon/populateCouponProducts, addMediaRow/collectMedia/updateCoverage, and the P3d wireUploadZone/wireUploadZones/nextNumberedRole) reference only existing helpers (authHeader, setStatus, escapeHtml, centsToDollars, deriveSlug, $) + in-scope vars; every new CSS id/class used in JS exists in the markup; the schema YAML + vercel.json stay valid.
- The tsc-clean + CommonJS + 11/12-function claims hold (no new api/*.ts; removed code isn't still referenced; the products(...) embed type cast compiles).

OUTPUT
- Ranked list: location, the mismatch, the corrected anchor/fix.
- The single most important fix.
- One-line verdict: READY TO BUILD or NEEDS ANOTHER PASS.
```

---

## Angle C — integration (repo + architecture)

```
You are a senior engineer doing a pre-build gap review. Effort: maximum. Do NOT change code or docs — output findings only (write them to v3_2_2_GAP_REVIEW_C.md).

⚠️ ROUND-3 RE-RUN — SCOPED, NOT A FRESH REVIEW. You returned READY TO BUILD on v3.2.1: you already traced the whole system end-to-end and cleared its integration. We're re-running you for ONE reason — between v3.2.1 and v3.2.2 we folded edits, and two landed in YOUR (integration) lane:
  (1) the WS6 cutover `update products … set quantity = 0` now ships COMMENTED in the Phase 6.1 migration (so `supabase db push` applies ONLY record_sale; the operator runs the UPDATE by hand once after the SELECT) — confirm the migration still leaves a consistent system and record_sale still applies cleanly;
  (2) coupon min_amount now rides the wire (handleCouponList) + renders in /admin (renderCoupons) + is read by the GPT off the opaque-200 raw body (NO schema edit) — confirm that cross-surface readback path composes and the no-schema-edit claim holds (ledger 27: the coupon Action 200s are opaque, so a new wire field is GPT-visible without a schema change).
The repo has NOT changed since your last pass (docs-only folds), so the ONLY integration surface that can have moved is those two. YOUR SCOPE THIS ROUND = confirm those two compose in the wider system. Do NOT re-trace the contracts you already cleared (refund idempotency, the relist embed, requireAdmin, chat-attach→render, the admin-redesign composition — all blessed last round), and do NOT surface polish — flag ONLY a genuine integration defect the v3.2.2 delta introduced. (The "whole build / don't shrink the scope" mandate below was for your FIRST pass — it exists so a reviewer doesn't wrongly EXCLUDE necessary parts of the build; you did that pass and cleared it, so this narrowed delta-scope is deliberate, not a violation.)

THE REVIEW LENS (both): (a) North Star — minimize Em's friction; every capability doable in BOTH /admin AND the GPT. (b) Broader mandate — the lens does NOT shrink the scope: the WHOLE build (assets/docs/archive/v3_2/v3_2_2_IMPLEMENT.md + BOTH assets/docs/archive/v3_2/v3_2_2_ADDENDUM_DESIGN.md and assets/docs/archive/v3_2/v3_2_2_ADDENDUM_TESTING.md, every element) for exclusively-executable / unvalidated-assumption / design-correctness gaps. Hunt INTEGRATION gaps through this lens: a locally-correct edit that, in the wider system, makes a by-chat/by-admin capability fail or leak, or breaks a render.

READ IN FULL + CO-DESIGN + FLAG-DON'T-ASSERT: read ALL provided docs end-to-end — don't ration tokens (you're at max effort, context is managed for you; grep/jump reading caused real mis-diagnoses last round). Within the v3.2.2 delta a gap is a gap whether it's a flaw in what we wrote OR something the delta SHOULD address but omitted — don't assume the build is mostly complete and you're only auditing what's there. When a finding depends on runtime/code behavior you can't directly see, FLAG it as needs-verification — do NOT assert it as broken (in prior rounds several "broken" findings were contradicted by the actual code — round-2's "critical data-corruption", and round-3's "an undeclared response field is invisible" (the coupon ops are opaque — description-only 200s — so the GPT reads raw JSON), "the chat-attach branch is unauthenticated" (auth runs at upload.ts POST top), and "the hero CSS tokens are missing" (they exist) — though note the round-3 multi-cart over-refund WAS real, so flag-don't-assert cuts both ways — and round-5 added THREE more code-contradicted false alarms: "#222 must be body text" (it's the accent-button + active-subtab background), "homepage.js may load synchronously" (it's `defer`), ".hero h1 may be a direct-child selector" (it's a descendant) — all now verified and recorded in the docs, so for a delta on a settled base prefer "I can't verify X from the docs" over "X is broken").

SETTLED BASE — don't re-litigate what's shipped: the current store (all of EVERLASTINGS_STORE.md + the repo as it stands today) is built, tested, and approved to go live — the fixed, proven substrate. This build (v3.2.2) is a DELTA on top. Review the v3.2.2 changes for gaps + whether they FIT the base; do NOT redesign or flag settled/shipped behavior — a finding must be about what THIS build adds/changes, or a real conflict it creates with the base.

CONTEXT
- Read assets/docs/EVERLASTINGS_STORE.md FIRST, then the three v3.2.2 docs. A FRESH agent applies the WHOLE build to this repo + tests on the dev preview. DESIGN INTEGRATION IS IN SCOPE (the admin redesign touches the same admin.js/index.html the functional WS1-3 edits touch — check the edits compose; the homepage hero swap must preserve the existing overlay/spotlight/glow/parallax + reduced-motion layers).
- LANDMINES (validate against reality, not training data):
  → LANDMINES = the **Settled — do not re-raise** ledger (entries 1-61) in the shared section above — you have this whole file in context. Validate each against the repo (verified findings from the 7 cold-A rounds + B/C/D rounds 1-2, not training-data guesses), FLAG-don't-assert, and do NOT re-raise any as a new finding. (High-frequency false-alarm classes: 7/8 refund amount+relist, 22/30/37 doc-lag+verified, 23 parity, 29/31 the char caps, 42 client-side filter, 44 multi-admin-deferred; round-1 folds 45-53; round-2 folds 54-61 — the `.media-row__head` class stacked in P5, the COMMENTED cutover UPDATE, links→`--c-accent` + pills-retoken-in-place, no `Refunded` legend swatch, the `PRODUCT_API_KEY` auth comment, coupon `min_amount` readback, the kept Upload heading, badge `--s-3` + skeleton `height:140px`.)

ANGLE C — integration. Hunt system-fit gaps:
- The refund: the action's optimistic orders.status flip vs the charge.refunded webhook (webhook.ts:60-89) — no double-write hazard, both idempotent; is_test scope holds; the relistPiece reuse of PUT /api/products {available:true} + POST /api/products/unarchive matches the real endpoints + change-detect behavior; the products(...) embed shape (LANDMINE 8).
- Parity: every capability genuinely drivable in BOTH /admin and the GPT (refund, coupons incl. scope-by-stripe_product_id + human dates, attach/by-link upload, MP4 config). Anything in one surface but not the other?
- Chat-attach: openaiFileIdRefs → the SAME pipeline; the by-link backstop intact; the positional/roles[] mapping yields filenames product.js's regex reads (LANDMINE 4); the /api/upload/attach rewrite + the dispatch branch.
- The admin redesign composes with WS1-3's same-file edits (the refund button, the coupons tab, the media editor) — no conflicting edits to the same blocks; the token literal-sweep doesn't break a selector something relies on; P0's state-filter reads the right product fields.
- Homepage: the versioned-key swap + the Lottie wrapper preserve every existing hero layer + reduced-motion + the real <h1>.

OUTPUT
- Ranked list: location, the integration gap, the concrete fix.
- The single most important fix.
- One-line verdict: READY TO BUILD or NEEDS ANOTHER PASS.
```

---

## Angle D — design-correctness (repo + design addendum)

```
You are a senior engineer + design reviewer doing a pre-build gap review. Effort: maximum. Do NOT change code or docs — output findings only (write them to v3_2_2_GAP_REVIEW_D.md).

THE REVIEW LENS (both): (a) North Star — minimize Em's friction; the /admin panel must let her do everything the GPT can, and feel genuinely polished + clear doing it. (b) Broader mandate — you are the DESIGN-CORRECTNESS lens that A (no repo) and B/C (code-fidelity/integration) under-weight: does the UI actually RENDER right, is it ACCESSIBLE, RESPONSIVE, and does it MATCH the design addendum's spec? The "columns-bug" class is your home turf — a spec that applies cleanly but renders wrong/incomplete (e.g. removing an inline style assuming a stylesheet rule wins the cascade — does that rule exist and win?). Build guide: assets/docs/archive/v3_2/v3_2_2_IMPLEMENT.md, addenda assets/docs/archive/v3_2/v3_2_2_ADDENDUM_DESIGN.md and assets/docs/archive/v3_2/v3_2_2_ADDENDUM_TESTING.md. 

READ IN FULL + CO-DESIGN + FLAG-DON'T-ASSERT: read ALL provided docs end-to-end — don't ration tokens (you're at max effort, context is managed for you; grep/jump reading caused real mis-diagnoses last round). Within the v3.2.2 delta a gap is a gap whether it's a flaw in what we wrote OR something the delta SHOULD address but omitted — don't assume the build is mostly complete and you're only auditing what's there. When a finding depends on runtime/code behavior you can't directly see, FLAG it as needs-verification — do NOT assert it as broken (in prior rounds several "broken" findings were contradicted by the actual code — round-2's "critical data-corruption", and round-3's "an undeclared response field is invisible" (the coupon ops are opaque — description-only 200s — so the GPT reads raw JSON), "the chat-attach branch is unauthenticated" (auth runs at upload.ts POST top), and "the hero CSS tokens are missing" (they exist) — though note the round-3 multi-cart over-refund WAS real, so flag-don't-assert cuts both ways — and round-5 added THREE more code-contradicted false alarms: "#222 must be body text" (it's the accent-button + active-subtab background), "homepage.js may load synchronously" (it's `defer`), ".hero h1 may be a direct-child selector" (it's a descendant) — all now verified and recorded in the docs, so for a delta on a settled base prefer "I can't verify X from the docs" over "X is broken").

SETTLED BASE — don't re-litigate what's shipped: the current store (all of EVERLASTINGS_STORE.md + the repo as it stands today) is built, tested, and approved to go live — the fixed, proven substrate. This build (v3.2.2) is a DELTA on top. Review the v3.2.2 changes for gaps + whether they FIT the base; do NOT redesign or flag settled/shipped behavior — a finding must be about what THIS build adds/changes, or a real conflict it creates with the base.

CONTEXT
- Read v3_2_2_ADDENDUM_DESIGN.md (WS4 admin token system + P0-P7; WS5 Lottie title + old-film hero) + the parts of v3_2_2_IMPLEMENT.md it depends on (WS2 coupons tab, WS3.7 admin media UX) + the repo (admin/index.html inline CSS :8-74, admin.js, styles.css hero region, index.html hero, product.js populateMedia). Also the FEEDBACK_ADMIN_v2_1_0.md screenshots. The bar: design ships as concrete-default + render-tune — judge "concrete enough to build + correct + accessible," NOT final pixels.
- LANDMINES (validate against reality, not training data):
  → LANDMINES = the **Settled — do not re-raise** ledger (entries 1-61) in the shared section above — you have this whole file in context. Validate each against the repo (verified findings from the 7 cold-A rounds + B/C/D rounds 1-2, not training-data guesses), FLAG-don't-assert, and do NOT re-raise any as a new finding. (High-frequency false-alarm classes: 7/8 refund amount+relist, 22/30/37 doc-lag+verified, 23 parity, 29/31 the char caps, 42 client-side filter, 44 multi-admin-deferred; round-1 folds 45-53; round-2 folds 54-61 — the `.media-row__head` class stacked in P5, the COMMENTED cutover UPDATE, links→`--c-accent` + pills-retoken-in-place, no `Refunded` legend swatch, the `PRODUCT_API_KEY` auth comment, coupon `min_amount` readback, the kept Upload heading, badge `--s-3` + skeleton `height:140px`.)

ANGLE D — design-correctness. Check:
- Admin token system: every literal mapped to a token; no raw hex left; the cascade actually wins (a removed inline style is replaced by a rule that exists + wins); product-state badges legible; the new coupon-list rows + media-editor rows + refund button inherit the tokens (or are flagged for the WS4 restyle).
- Accessibility: the Lottie title keeps a real <h1> (SEO + SR) + a reduced-motion fallback to the static styled title; no-JS / 404-JSON still shows the <h1>; the old-film hero preserves the reduced-motion poster fallback; focus states + contrast in the admin redesign.
- Responsive: the admin mobile breakpoint (none today) stacks the forms/cards/order-cards without overflow at ≤640px; the structured media editor + coupon form are usable on a phone.
- Spec-match + completeness: P0 (nav/back + state-filter), P1-P7 are each concrete enough to build; the homepage integration (wrapper + a11y CSS + init; versioned-key swap + 3 URL edits) is concrete; render-tune items are honestly labeled (not pretending to be final).
- Honesty: any place the design reads "done" but is actually undecided / would render wrong / skips a state (loading/empty/error).

OUTPUT
- Ranked list: location, the design-correctness gap, the concrete fix.
- The single most important fix.
- One-line verdict: READY TO BUILD or NEEDS ANOTHER PASS.
```
