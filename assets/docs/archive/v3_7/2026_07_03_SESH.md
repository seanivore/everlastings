# 2026-07-03 — v3.7 B/C/D gate setup + pre-B/C/D breadth round → v3.7.1

Continues the CCP redesign build (see `v3_5/2026_07_02_SESH.md` for the v3.5→v3.6 A-gate history). This session: retired the Agent-SDK A-gate experiment, returned the gap-review loop to **human-facilitated Claude Code**, set up `v3_7/` for the B/C/D gate, and ran the pre-B/C/D breadth round.

## Model experiment (the reason this session matters beyond the folds)

The SDK A-gate ran the WHOLE loop on **Opus 4.8 MAX** and still fumbled (obvious-fold approvals, fold-then-ask, forgotten version bumps). Diagnosis (Sean): **the harness is the load-bearing variable, not the model tier** — SDK instances lack the terminal basics + the ambient seriousness memory/`.agent`/todos/plans/chat give a Claude Code instance. So: back to Claude Code, and split the roles by benchmark.

- **Reviewers = Sonnet 5 · MAX** (knowledge-work parity with Opus 4.8: GDPval 1618 vs 1615, HLE-tools 57.4 vs 57.9; same 1M ctx; ~2.5× cheaper → the rate-limit unlock). Effort = MAX because a context-less peer only reads everything if pushed.
- **Orchestrator = Opus 4.8** (agentic-coding + discipline: SWE-bench Pro 69.2 vs 63.2, Terminal-Bench 82.7 vs 80.4). Raise *effort* if a fold feels thin, don't switch models.
- Codified in **DEV_RULES v4.2.0** (§Gap-Review Gate → "Model & effort selection" + a dated calibration table).

**Spawned-peer-as-gate finding:** the 2 breadth peers came back **gate-grade** — read actual backend code, cross-checked line numbers against the live tree, caught real load-bearing issues, cleared the Vercel-budget/byte-math/migration checks. This supports collapsing the old multi-*window* ceremony into a single orchestrated chat: a spawned peer has clean isolated context (the property that made "fresh window" matter), and the Sonnet-reviewers/Opus-orchestrator split gives **decorrelated blind spots** the old all-Opus fresh-windows never had. (Residual edge for a true cold-A: out-of-ecosystem read — solvable via a tool-blocked peer or Claude.ai Cowork; moot here, A is closed.)

## Setup (commits `eb0a7dc`, `cb0fcb0`)

- **COPY** converged `v3_6_7_*` → `v3_7/v3_7_0_*` (phase crossing = COPY, not git mv; `v3_6_7_*` stays frozen in `v3_6/` as the A-gate record + its GAP_REVIEW trail). Headers/banners bumped, A-Type→B/C/D reframed.
- **Re-oriented `REVIEW_PROMPTS`** for Claude Code: dropped the SDK courier framing, added a "How to run these blocks" note, reframed gate status (A CLOSED; B/C/D re-open for a FULL pass because the A-round-1 folds ledger 57–72 landed in all three lanes), fixed the stale `v3_6_6_ADDENDUM` ref, output contract writes the standing GAP_REVIEW file + replies. Ledger 1–72 verified identical across all four A/B/C/D blocks.
- **Launch kit** (`REVIEW_LAUNCH`) with per-window Sonnet-5·MAX instructions + 2 self-contained breadth charters.
- Orchestrator clean-read of the v3.7.0 triplet: coherent, previewToken crash-fix intact, sold-derivation harmonized, §10.1c agrees across IMPLEMENT/TESTING.

## Breadth round → v3.7.1 (this session's fold)

2 Sonnet-5·MAX peers (journey + integration), both **NARROW**, validated against the code before folding:

- **journey #1 [LOAD-BEARING] — checkout-identity freeze (§2.7a).** `§2.2` (pause = unpublish to draft) exposed that the `FROZEN_AFTER_PUBLISH` guard keys on *current* `is_published` (`products.ts:390`), so a paused-but-ever-published piece re-opened checkout_name/description/image; an edit stuck in the DB while `syncProductToStripe` short-circuits on the existing `stripe_product_id` → silent **site↔Stripe desync**; relist also re-stamped `published_at`. **Owner decision (Sean, GWYG): freeze once EVER-published** (guard fires on `published_at != null`) + preserve `published_at` on relist. Storefront presentation stays editable; only the Stripe checkout snapshot locks. The deliberate confirmed **"Reissue checkout identity"** (a Stripe-product-UPDATE path) is a **named DEFERRED feature**, not built — keep the build lean, don't touch the Stripe catalog willy-nilly.
- **integration #1 [LOAD-BEARING] — handler append-stack.** `handleActiveSale` (§4.2.b) + `handleActivityLog` (§8.1d.b) both append in the `handleCouponList`→`coupon_deactivate` gap → added the coordination note (WS4 first, WS8 re-anchors on the stable comment; a stack, not a collision).
- **journey #2 [LOAD-BEARING] — media partial-failure (§5.4c.i).** Clarified: the terminal PUT fires with the partial (succeeded) `p.images` before stopping → no R2 orphan on reload.
- **integration #2 [LOAD-BEARING] — GPT `Optl.`↔schema.** Instruction line 4 still frames 6 of 7 publish-required fields as `Optl.`; §10.1c bets the schema `Required to publish.` overrides it → strengthened TESTING 6b to VERIFY proactive collection (not just the reactive 400) + documented the byte-budget fallback (rework line 4 via §10.2b's freed clauses if the test fails).
- **2 polish coherence fixes:** stale-quantity caveat now notes `computeState()` misclassifies too (both surfaces, one backfill); §4.1.d de-dangled (GPT gets `auto_apply` in raw JSON regardless — no missing WS10 phase).
- **3 polish DEFERRED (not built — Sean's "less work" call):** Related-Havens mini-card struck pricing, GPT scheduled-publish push-notif, Orders-nav load-blink.
- **Ledger 73–77** appended to all four blocks; living docs renamed `v3_7_0_*` → `v3_7_1_*`; breadth trail kept standing.

## Next

**Compact forward → run B/C/D** (Sonnet 5 · MAX peers) against the **v3.7.1** triplet → validate → fold → converge. Then the MAJOR **v4.0.0** execution-copy cut in `v4_0/` (architecture-removal consolidation), then execute. Deferred final phase: as-built doc-sync (STORE / STORE_ADMINISTRATION / README).
